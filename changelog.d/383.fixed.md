- Fixed two `rglob`/`glob` walks in `doctor.py` that silently swallowed `PermissionError` for a directory they could not enter (#383), and three narrower unguarded `is_file()`/`is_dir()` sites of the same class found while censusing the rest of `scripts/` for it — two more in `doctor.py`, one each in `rename_changelog_fragment.py` and `release_delta.py`.
