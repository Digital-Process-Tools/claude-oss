- Fixed two `rglob` walks in `doctor.py` that silently swallowed `PermissionError` for a subtree they could not enter (#383), and three narrower unguarded `is_file()`/`is_dir()` sites of the same class found while censusing the rest of `scripts/` for it — one each in `doctor.py`, `rename_changelog_fragment.py` and `release_delta.py`.
