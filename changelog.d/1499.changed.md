The read-cost and CI-trust rules now ship to every repository rather than
living only in this one's `CLAUDE.md`. `agents/developer.md` gains the 20,000 B
read cap and the rule against re-reading a file already in context;
`agents/sub-manager.md` gains the measurement showing whose context a tick
actually spends; and the `CLAUDE.md` written into a scaffolded repository gains
an "If an agent is doing the work" section covering trust-CI-rather-than-replay,
reading CI's answer rather than assuming it, and reading narrowly and never
twice.
