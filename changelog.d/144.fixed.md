- `/oss:doctor` counted a layer's `00-README.md` as a rule, so a layer that documented itself
  reported one more rule than it has — beside a drift check in the same function that had
  already filtered the same file out. Nothing showed it while no layer carried a record
  (#144).

