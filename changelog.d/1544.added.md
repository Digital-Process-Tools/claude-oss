- Split a tick's own CI-wait + review step out of `oss:sub-manager` into a new spawn, `oss:tick-review` -- part of #1544's step 2 (not the whole issue: merge and accounting/handback stay unsplit). `pr_green.py`'s wait and `skills/manager/phases/review.md`'s checklist now run in a throwaway context instead of the sub-manager's own long-lived one.
