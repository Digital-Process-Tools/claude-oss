- Collapsed pick-the-work's four hand-composed dispatch steps into two, the script half
  (#1143): `lane_setup_label.py` is deleted, and `fleet_label`/`agent_call` are folded directly
  into `lane_setup.py`, whose `--claim` mode now renders this lane's own fleet-view label -- or,
  given `--subagent-type` and `--brief`, the whole `Agent(...)` call -- from the issues that
  `--claim` call **actually holds** afterward, never from the issue and `--claim-also` values
  requested. A companion whose assignee write came back `could-not-claim-assignee` or
  `already-claimed` is silently excluded from the count: a lane whose third issue fails to claim
  now renders `x2`, not a confidently wrong `x3`. `--label` is unchanged and stays the path for a
  lane composed some other way. `brief_schema.py` is renamed to `lane_setup_brief_schema.py` and
  loses its own standalone `main`/argparse CLI, following the "No longer a standalone CLI"
  precedent `select_issues_rank.py` and `select_issues_preflight.py` already carry from #1069:
  `lane_setup.py --claim --subagent-type ... --brief <path>` is the one entry point that reaches
  it now, checking the brief before rendering the `Agent(...)` line and refusing to render it on a
  finding in one of the brief's four **structural** elements, while a **presence-only** finding is
  printed and the line still renders -- `a brief that passes is not a brief that was reviewed`
  still prints on every check. The five prose sites in `skills/manager/phases/dispatch.md` and
  `phases/tick-order.md` that still name the old `--label`/`brief_schema.py` calls by hand are a
  second, deliberately separate round (held by open PR #1141); this change is the script half
  only.
