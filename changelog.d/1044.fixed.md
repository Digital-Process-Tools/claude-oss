- Confirmed #1044's own bug shape -- a lane-fill adjacency check pointed at
  sibling running lanes instead of the open board, so a truthfully short lane
  read `no-adjacent` on the strength of a measurement that never looked at
  the board -- no longer reproduces on the live dispatch path. `select_issues.py`'s
  own `select()`/`select_fleet()` already compose the real companion sweep
  (`select_issues_companions.suggest_companions`) over the open board rather
  than over sibling lanes, and `select_issues_rank.check_lane(...,
  adjacent=...)` already refuses a `no-adjacent` claim contradicted by a real
  candidate count (#918/#921). No production code changed; added a
  regression fixture (31 open issues, two of them genuinely adjacent to a
  dispatched lane's claimed files) pinning that the real, unmocked sweep
  finds them, and that a truthfully isolated lane on the same board still
  reports `no-adjacent` correctly.
