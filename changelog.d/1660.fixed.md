- Raised the `pytest` job's `timeout-minutes` from 20 to 30 in `.github/workflows/tests.yml`:
  the `pytest (windows-latest, 3.12)` leg was cancelled again after #1659's own raise, twice
  back to back on the same commit, because the suite's own reported runtime had grown from
  ~873s to ~1176.55s -- within 24 seconds of the 20-minute cap. Also adds
  `tests/posthang_diagnostics_1660.py`, a periodic thread-stack dump armed once a session
  finishes, since both cancellations show the process still alive, blocked in the main
  thread, several minutes after pytest's own summary line already reported every test
  passing -- the root cause is not yet identified, and this gives the next occurrence a
  real answer instead of only a bare `KeyboardInterrupt` traceback (#1660).
