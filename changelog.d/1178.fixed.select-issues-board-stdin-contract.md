- `skills/manager/phases/dispatch.md` and `skills/manager/phases/tick-order.md` both pointed a
  session at `scripts/select_issues.py --board` right next to the true statement that the script's
  *default*, no-flags mode takes no input at all -- and `--board` is a separate, older CLI mode
  that does read a board-shaped payload on stdin. A reader who had just read the correct "no stdin"
  sentence about the other mode drew the wrong conclusion at the `--board` call site and got a
  `stdin: not valid JSON` failure for their trouble, observed three times in one tick. Both call
  sites now say `--board`'s own stdin contract explicitly (#1178).
