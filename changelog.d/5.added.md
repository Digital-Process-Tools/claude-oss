- `scaffold.py` had no way to preview a generated file's content, though `/oss:scaffold`'s own
  instructions require showing it before writing. `--show` now prints the body of every file the
  plan would create, or `--show PATH` for one file — nothing is written, and it refuses to run
  together with `--apply` (#5).
