- Extended the supertool-rule-body sync guard (#577) to cover all three
  `tools`-dimension jit rules instead of just one:
  `tests/test_supertool_rule_sync_577.py` now derives the file/constant pairs
  to compare from `oss_rules.RULES["tools"]` itself, so a rule added to that
  dict later is covered automatically rather than needing a hand-added test
  function (#888).
