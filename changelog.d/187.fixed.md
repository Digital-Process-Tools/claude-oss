- `/oss:tick` step 2 now orders the two board readings the loop was deciding from without ever
  taking: the watcher fleet, via the read-only `radar:--state` probe ahead of any bare `radar` that
  heals and forks pollers, and the worktree board via `git-worktrees` wherever `worktree_root` is
  set. Both omissions produced an absence that read as a clean result — a fleet nobody checked
  renders as a quiet channel, a worktree board nobody read renders as no worktrees — and the step
  now carries the three-state reading rule with it, so `cannot tell` cannot become `idle` and
  `merge unknown` cannot become `merged` in the place the loop actually reads (#187).
