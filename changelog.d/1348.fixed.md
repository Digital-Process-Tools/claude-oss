- Add `trap.d/README.md` to `CLAUDE.md`'s ownership table (#1348), closing the gap where a
  file `scripts/scaffold.py`'s `OWNED` set has replaced wholesale since #1302 read, by
  omission, as ordinary user content. A new test,
  `tests/test_claude_md_ownership_table_1348.py`, derives the expected `ours` row from
  `scaffold.OWNED` and fails loudly on the next such drift.
