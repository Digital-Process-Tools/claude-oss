- Added a jit-context rule (`.claude/jit-context/tools/01-oss/tree-snapshot-compare.md`,
  shipped in every managed repository's generated layer) for the review phase's
  `tree_snapshot.py` mutation check: put the `cd` on the same call as the compare, write the
  before-snapshot inside the worktree rather than the shared scratchpad, and treat
  `could-not-compare` as a third verdict that is never `clean` -- three traps observed at
  #456, #539 and #527 (#1105).
