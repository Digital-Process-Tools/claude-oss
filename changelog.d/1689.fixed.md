- `setup.md` and the `scheduler-step` wrapper it and `run.md` both spawn now assert
  `${CLAUDE_PLUGIN_ROOT}` resolves to a real plugin checkout before trusting it, stopping with
  `SETUP: could-not-run` instead of silently probing the wrong tree under `--plugin-dir` or a
  project-scoped install run from another directory (#1689).
