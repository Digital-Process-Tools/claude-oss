Release audit (round 2, v0.27.1..HEAD) found: tests/import_closure.py:32-53,56-91 -- `_call_candidates`
returns `[]` both when a line is not an import call at all AND when it IS
`importlib.import_module(...)` but with a non-literal argument the resolver declines to follow.
`local_import_closure` returns only the resolved `seen` set, with no companion list of names it
could not resolve, so a caller reading the closure sees it as complete when it may not be.
Live instance: scripts/borrowed_authority.py:224 is `importlib.import_module(module_name)` on a
variable, which this resolver cannot follow. tests/test_import_closure_1236.py:5 states no seed
path reaches `borrowed_authority` "today" -- that "today" is the whole exposure: nothing catches it
if a future seed path starts reaching that module. The two callers relying on completeness are
`_REAL_MODULES` in tests/test_workspace_routes_launcher_1155.py:81 and
tests/test_workspace_doctor_route_receipt_1064.py:66. Ranked `misreports` (non-blocking),
reasoned rather than observed (suite not run).
