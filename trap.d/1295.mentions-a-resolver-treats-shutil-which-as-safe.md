Gate 3 round-2 audit for the v0.28.0..HEAD delta (release toward v0.29.0, dispatch token
gate3-r2-bb1ee13dd139) found tests/test_bare_gh_git_spawn_sweep_1165.py's `_mentions_a_resolver`
predicate (added this delta) returns True for ANY call whose name or attribute contains `which`,
case-insensitively -- including bare `shutil.which`. scripts/gh_which.py's own module docstring
(lines ~13-20) states that `shutil.which` alone does NOT close the Windows gap it exists to fix
(a planted `gh.cmd` can still win); the resolver has to be `gh_which.safe_which`, not any
"which"-shaped call. So a wrapper shaped
`def _run(cmd): b = shutil.which(cmd[0]); return subprocess.run([b] + cmd[1:])` is classified
safe by the sweep, and any literal-`["git", ...]` call site into it would go unflagged (sweep
case 1 does not catch it either, since its argument is not a literal git/gh string). No live
instance found in scripts/*.py today -- this is a coverage gap in the guard, not a shipped
bypass.

Fix shape: narrow `_mentions_a_resolver` to require `safe_which`/`gh_which` specifically, or add
a companion check that a bare `shutil.which` result feeds a spawn.

Confirmed by: reading _mentions_a_resolver against gh_which.py's own docstring at HEAD b95ae4b;
reasoned rather than a constructed live exploit.
