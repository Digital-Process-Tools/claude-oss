While fixing #1649 (agents/doctor.md's on-default branch-protection gate, plus pinning the two
`Agent(subagent_type: "oss:doctor")` spawns in commands/run.md and commands/doctor.md to
`run_in_background: false`), a spawned Explore reviewer found a third bare call in the same family
that this issue's own scope did not cover: `commands/run.md`'s step spawning
`Agent(subagent_type: "oss:releaser")` (around line 151 at the time of this note) still carries no
`run_in_background` pin at all.

Left unfixed here on purpose -- it is a different subagent (releaser, not doctor) with a different
concurrency profile than the one #1649 argues about, and #1649's own issue body never raised the
releaser spawn as a concern. Fixing it felt like reaching outside this issue's blast radius rather
than finishing it.

Worth a grep sweep across `commands/*.md` and `agents/scheduler-step.md` for every remaining bare
`Agent(subagent_type: ...)` call with no `run_in_background` token at all, the same class #1586
and #1649 have now each fixed one instance of, rather than fixing them one grep-hit at a time
across separate issues.
