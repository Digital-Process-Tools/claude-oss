Gate 3 release audit (v0.30.0, dispatch gate3-r2-68183c5a1a90, round 2, range
v0.29.1..HEAD), class misreports, non-blocking.

scripts/tree_snapshot.py:264-279 -- SNAPSHOT_ARTIFACT_RE =
r"(?:^|/)(?:[^/]*-)?before-snapshot\.json$" excludes matching untracked paths
from both `added` and `removed` in compare(), and the regex matches at any
depth. A lane that writes e.g. src/anything-before-snapshot.json during a
review is invisible to the mutation detector that exists to catch exactly
that. The docstring is honest about the limitation ("no stronger a guarantee
than that"). Fix under consideration: anchor the exclusion to the specific
snapshot path the caller passed, rather than to a name shape matched anywhere
in the tree.
