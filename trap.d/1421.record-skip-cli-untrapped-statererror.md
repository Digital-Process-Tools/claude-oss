Gate 3 release audit (v0.32.0, round 2, dispatch token gate3-r2-891d2d27a502) found:
`scripts/next_action.py:806-808` (`_record_skip_cli`, whose own docstring says "Never raises past
this point: every failure is a printed FAIL:") catches only `ValueError`, but `record_skip` ->
`oss_state.append` raises `oss_state.StateError` (base `Exception`, `oss_state.py:323`) when
`"took X over Y (<reason>)"` exceeds `MAX_DECISION = 200` (`oss_state.py:48,731`), or the state
directory is unwritable.

`commands/run.md:75` hands `--reason "<why>"` to a session writing free-form prose; roughly 170
characters of reason (well within what a session might plausibly write to explain a skip) produces
an uncaught traceback instead of the `FAIL:` line the procedure tells the session to read. Exit is
still non-zero, so nothing downstream is silently misled, but the printed diagnostic is a Python
traceback rather than the documented contract.

Ranked `misreports` -- non-blocking. Fix shape: widen the except clause in `_record_skip_cli` to
also catch `oss_state.StateError` (or the common base both share) and print it through the same
`FAIL:` path as the `ValueError` arm.
