Gate 3 round 1 release audit (v0.32.0..HEAD, dispatch token gate3-r1-a3cefee77a71) found:
scripts/oss_rules.py:794-795 and .claude/jit-context/tools/01-oss/merge-gate.md:28-29 (curate
commit 3db5581) scaffold the TOOLS_MERGE_GATE rule with a literal `git fetch && git merge
origin/main` instruction. `default_branch` is a per-repo .oss.json config key
(scripts/oss_rules.py:352), and this same delta's commit 43785ca
(skills/manager/phases/dispatch.md:563-566) describes a managed repo whose trunk is not `main`.
`ships-local-state` was considered and rejected by the auditor: `main` is not true of exactly one
checkout, it is a default wrong for a whole class of repos; the fix (name <default_branch>) is the
same either way. Ranked `misdirects` -- can ship behind a trap.d fragment, no embargo.

What settles it: what `git merge origin/main` actually does in a managed repo whose default branch
is e.g. `master` -- a clean refusal, or (worse) a merge of a stale non-default `main` if one
happens to exist in that repo. Fix: substitute the repo's own default_branch into the scaffolded
rule text rather than hardcoding `main`.
