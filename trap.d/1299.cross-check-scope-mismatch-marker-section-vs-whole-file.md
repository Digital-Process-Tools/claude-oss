Gate 3 round 1 audit for v0.29.1 (dispatch token gate3-v0291-r1-a685778e7634) found
commands/release.md's marker gate now names
tests/test_cohort_citation_order_1220.py::test_extract_cited_cohort_on_this_repos_own_claude_md_cross_checked
(#1299) as one of the two guards to run before a release commit, but that test's own
independent-oracle derivation (`start = text.index("Cohort freeze: ")`) searches the WHOLE file,
while `cohort_citation_order.extract_cited_cohort` (the function it cross-checks) deliberately
scopes its own search to `_marker_section` -- only inside "## What is not proven yet" -- per
#1269, because this repo's prose routinely narrates past cohort counts elsewhere. The moment
CLAUDE.md gains any earlier "Cohort freeze: " mention outside that section (exactly the kind of
historical narration #1269's own comment describes as routine), the two halves would read
different sentences and this cross-check test could fail on a genuinely correct marker, or
silently agree with the wrong one by coincidence. Both currently agree (verified: `_MARKER_RE.
findall` returns `[('25','16')]` against both the section and the whole file, and there is
exactly one "Cohort freeze: " occurrence in the file today) -- this is a latent scope mismatch,
not a live bug.

Fix shape: change the test's own oracle derivation to use `cco._marker_section(text).index(...)`
rather than the whole-file `text.index(...)`.

Confirmed by: reading extract_cited_cohort/_marker_section against the cross-check test's own
oracle derivation at HEAD a887298; reasoned rather than a constructed failing case (no suite run).
