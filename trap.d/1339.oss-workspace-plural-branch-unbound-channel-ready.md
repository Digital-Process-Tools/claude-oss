Gate 3 audit round 1 (v0.30.0, dispatch gate3-6a7d9d04e7c7, range v0.29.1..HEAD), class
fails-to-preserve, non-blocking (the ranking table has no row for "the tool stops working";
unranked was considered and rejected because a row that fits -- fails-to-preserve -- should be used
before inventing an absence).

bin/oss-workspace:1452-1465 (`plural` branch)'s own comment asserts `channel_ready` "is already 0
here -- initialised once, above, before this whole if/elif/else". False about its own file:
`channel_ready=0` is at :1496, inside the `else` arm; `channel_ready=1` at :1429 is inside `single`;
the `fi` closing the three-way conditional is at :1578, and :1604 reads `$channel_ready`
unconditionally. `set -eu` is in effect from :50. On the plural path the variable is never
assigned, so the shell dies at :1604 before `exec claude` -- no session at all -- while the
branch's own stderr claims "Opening WITHOUT the channel flag". One-line fix: hoist
`channel_ready=0` above :1387.
