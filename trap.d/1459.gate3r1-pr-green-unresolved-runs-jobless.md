Gate 3 round 1 release audit (v0.32.0..HEAD, dispatch token gate3-r1-a3cefee77a71) found:
scripts/pr_green.py:249-286 (_runs_on_commit, commit eddfc7d) collects every run on the sha
regardless of status/conclusion, and _unresolved_runs (pr_green.py:330-370) holds the verdict at
`pending` for any PR-event run with zero jobs. Reasoned, not observed: a `completed` run that never
produced a job (e.g. cancelled while still queued) would hold `--wait` at `pending` forever rather
than resolving to `green`/`red`, since nothing distinguishes "not yet reported jobs" from
"completed with none". Safe direction (never falsely green), but a wait that cannot end. Ranked
`misreports` -- can ship behind a trap.d fragment, no embargo.

What settles it: whether `.../actions/runs/{id}/jobs?filter=all` can actually return `[]` for a
`completed` run (needs a real reproduction, not just reasoning). If yes, filter on
`status != completed` before counting jobless runs as unresolved, so a genuinely completed-but-
jobless run resolves rather than hanging `--wait --timeout` forever.
