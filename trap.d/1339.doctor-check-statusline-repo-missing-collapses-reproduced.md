Gate 3 audit round 1 (v0.30.0, dispatch gate3-6a7d9d04e7c7, range v0.29.1..HEAD), class misreports,
non-blocking. Re-derived independently; same defect already logged at
trap.d/1334.doctor-check-statusline-repo-missing-collapses.md from the prior round -- recording the
reproduction here rather than silently dropping it since it is still present on this delta.

scripts/doctor_check_statusline_unknowns.py:377-378: `if statusline is None or not repo: cache =
None`. A .oss.json missing `repo` collapses onto the identical `cache = None` as "the cache was
never written". channel_cause (:133-164) then returns not-asked and _report_channel prints the WARN
whose remedy is --refresh, which also resolves nothing without `repo`. The WARN cannot be cleared by
the command it names. `repo` is in oss_config.REQUIRED_KEYS and non-nullable, but doctor is
precisely the tool run against a config that fails validation, so the arm is reachable.
