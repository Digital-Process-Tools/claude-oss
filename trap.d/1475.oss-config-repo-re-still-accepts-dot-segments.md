Gate 3 release audit (v0.33.1..HEAD, round 1, token gate3-r1-3bf37232ab6d5c4f) found
scripts/oss_config.py:255-264's REPO_RE, tightened by #1475 (272f773) to refuse `?` and `#`,
still accepts `..` and `.` path segments that two sibling guards it is meant to agree with
already refuse: doctor._malformed_repo and statusline._malformed_repo both explicitly check
".." in repo.split("/") and reject it; oss_config.repo_problem does not and accepts
'../..', 'a/..', './x'. The downstream call this feeds is cohort_freeze._resolve_repo_slug
(scripts/cohort_freeze.py:673-691), which routes a tracked, contributor-editable .oss.json
`repo` value through repo_problem alone before building
repos/{}/git/refs/tags/{} for a tag-creation API call. Probed the downstream effect: `gh api
'repos/../../zen'` returns a 404 body (Not Found), not the /zen response, so the traversal
does not currently redirect the call -- gh sends the literal malformed path and GitHub
404s. A leading `-` also still passes REPO_RE; scripts/lane_setup_claim.py:586-593 already
records that one as measured and accepted for `gh --repo`, so it is not new here. Fix:
align REPO_RE (or repo_problem) with the ".." segment check doctor.py and statusline.py
already carry, so all three guards agree. Ranking: misreports, non-blocking (probed and the
traversal does not currently escape the call).
