Gate 3 release audit (v0.33.1..HEAD, round 2, token gate3-r2-bcc87282b35641f8) found
scripts/workspace_routes.py:186-212 with scripts/trap_curate.py:122-215: when the checkout
is off the default branch, curate_count reads origin/<default_branch> via git ls-tree, and
nothing on that path fetches -- so a confident fragment count is attributed to the default
branch and is only as fresh as the last fetch, with no freshness signal in `why`. This is a
narrower residual of the bug #1476 fixed, arriving through a different door. Exercised:
trap_curate.waiting_at_ref('.', 'v0.33.1') -> state=waiting count=7,
waiting_at_ref('.', 'HEAD') -> state=waiting count=18 -- two refs, two counts, both
rendering as an ordinary waiting answer with no staleness signal; the failure arm works
(a nonexistent ref -> could-not-read). Grep for `fetch` in workspace_routes.py,
trap_curate.py and next_action.py returns 0 results. Not exercised end-to-end (building a
stale-remote fixture needs `git commit`, refused by this repo's raw_command_guard when
probed from inside the release session) -- the downstream consequence is reasoned, the two
halves above are observed. Ranking: misreports, non-blocking.
