Gate 3 round-2 audit for the v0.28.0..HEAD delta (dispatch token gate3-r2-bb1ee13dd139) found
tests/test_gh_git_resolution_arms_1295.py:99-140, the only drift guard between
statusline._safe_which and gh_which.safe_which, compares the two walks against one directory
holding one binary with PATH set to just that directory. Every divergence shape that would
actually matter is unexercised: PATHEXT candidate ordering, the empty-PATH-entry ->
os.curdir normalisation (gh_which.py:61-65), the normcase/abspath seen-directory dedup, and
multi-directory precedence. On POSIX both walks trivially land on the one binary however far
apart the two implementations have actually drifted. The test's own docstring calls it "the
cheapest check available today that the two still agree", which overstates what a
single-directory, single-candidate fixture can actually fail on.

Fix shape: extend the fixture to a multi-directory PATH with PATHEXT variation (at least on the
Windows-shaped assertions), so the sync test can actually catch a divergence in candidate
ordering or dedup logic rather than only in the trivial resolved-path case.

Confirmed by: reading test_gh_git_resolution_arms_1295.py:99-140 against gh_which.py's own
PATHEXT/curdir/dedup logic at HEAD b95ae4b; reasoned rather than a constructed failing case.
