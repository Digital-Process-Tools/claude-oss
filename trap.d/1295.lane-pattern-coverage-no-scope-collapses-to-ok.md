Release v0.29.0 gate 3, round 1 audit (token 3e96e2b2f6e6d380), class A finding, non-blocking
(misreports). scripts/doctor_check_lane_patterns.py:66-81, after the #1252 fix:

    if problem is not None:
        message += " coverage could not be counted ({}).".format(problem)
    elif count:
        message += " {} tracked-like file(s) ...".format(count)

uncovered_count is None with problem is None means "no lane resolved a single file, so no scope was
established at all" -- a genuinely different state from count == 0 ("scope established, nothing
uncovered"). Both fall through the same elif count: branch and print the identical bare OK line.
lane_pattern_coverage.py's own module docstring says these two "must not collapse to the same
number", and here they still render as the same message. Confirmed by monkeypatching
_walk_all_files to return (None, "PermissionError: ...") and reading back a clean-looking OK line
where a walk failure should have surfaced. Worth a real "no-scope" third message branch if this
ever fires for a repo whose labels.lane_patterns resolves nothing.
