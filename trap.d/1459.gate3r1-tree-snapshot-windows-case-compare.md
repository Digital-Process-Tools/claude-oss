Gate 3 round 1 release audit (v0.32.0..HEAD, dispatch token gate3-r1-a3cefee77a71) found, platform
band D, informational (windows-latest x 3.12 is already covered in the push matrix per
.github/workflows/tests.yml:72-75): scripts/tree_snapshot.py:278-297 (_root_relative_path, commit
e8ddcaf) does a case-sensitive string-prefix compare between --before's spelling and snapshot()'s
recorded `root`. Reasoned, not observed: a drive-letter-case or short-name spelling difference on
Windows would fail to resolve, the fallback then compares an absolute path against a relative
porcelain line, and the snapshot artifact gets reported as `mutated` when it was not. Ranked
`misreports` -- can ship behind a trap.d fragment, no embargo.

What settles it: a real Windows repro comparing two differently-cased spellings of the same root
path through _root_relative_path. If confirmed, normalize both sides (os.path.normcase or
equivalent) before the prefix compare.
