Release audit (round 2, v0.27.1..HEAD) found: scripts/oss_config.py's `effective_lane_labels` is
introduced as "the one place a caller derives this answer", then hand-mirrored into vendored
scripts/statusline.py:2144-2178 (which legitimately cannot import oss_config, per #653). No test
compares the two implementations' output over the same inputs:
tests/test_effective_lane_labels_1181.py exercises only `oss_config`'s version, and
tests/test_statusline_lane_other_no_lane_1181.py:73 names the function in a test name and
docstring but asserts hand-written expectations, never the other implementation's actual output.
This is exactly the two-copy-drift shape tests/test_supertool_rule_sync_577.py was written to
mechanize for a different pair (the supertool rule body). Fix: a test asserting the two functions
agree over a shared set of inputs, the same shape #577 already uses. Ranked `misreports`
(non-blocking), reasoned rather than observed (suite not run).
