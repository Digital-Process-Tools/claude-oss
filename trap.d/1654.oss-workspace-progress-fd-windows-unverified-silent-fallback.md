Release-auditor finding (round 1, v0.40.0..HEAD, dispatch token 9230a81691d1), ranked `misreports`,
platform band (D/F/H), non-blocking per skills/manager/phases/findings.md's table.

`bin/oss-workspace:291-310` -- `exec 3>&1` plus `--progress-fd 3` is unverified on Windows across the
MSYS/Python boundary, and `scripts/plugin_update.py:1063-1103`'s only failure arm is silent.
`os.fdopen(progress_fd, "w", closefd=False)` failing sets `progress_writer = None`, after which
`update()` is called with `progress=None` -- so "the descriptor could not be opened" and "the run
debounced and streamed nothing" render identically, which is the behaviour the same block's own
comment relies on for the common path. The delta's own merged rule,
`.claude/jit-context/paths/00-manual/windows-subprocess-resolution.md`, already states the reach
question as unconfirmed, so this is a known gap rather than a fresh discovery -- but the code that
would tell the two apart is not there.

Coverage: not covered. Read from `.github/workflows/tests.yml:119-122` (matrix
`os: [ubuntu-latest, macos-latest, windows-latest]` x `python-version: ["3.9".."3.12"]`) and
`:345-352`: the only leg that touches `bin/oss-workspace` is the `shell` job on ubuntu-latest, and it
only `bash -n`/shellchecks the file -- no leg executes it, on any platform.

Claim about the fd-3 hang fix itself is reasoned, not observed -- the test suite was not run by this
audit. Introduced by PR #1654.
