Gate 3 round 2 audit for v0.29.1 (dispatch token gate3-v0291-r2-12ef276f88c6) found
commands/release.md's step 1 (the cohort_citation_order.py gate) describes `could-not-check` as
fine and enumerates its causes as "(no state file, or a malformed --at)". That enumeration is
incomplete: exercised both directions with a valid --at, `cohort citation order: could-not-check
-- no cohort citation found in the marker -- nothing to check` is a THIRD, distinct cause -- a
marker whose citation sentence could not be parsed at all (exactly the shape v0.29.0's own
paraphrased marker produced, #1299) -- and it renders with the identical state and exit code as
the two causes step 1 already names. A reader who branches on the state alone (which is what the
paragraph tells them to do) cannot tell "the marker is intact but the state file lacks a record"
from "the marker itself is unreadable" without reading the reason string, which the paragraph
does not say to do.

Fix shape: either name the third cause in the enumeration, or add a sentence telling the reader
to check the reason string rather than only the state name.

Confirmed by: running cohort_citation_order.py against both the current CLAUDE.md and v0.29.0's
own (git show v0.29.0:CLAUDE.md) and comparing the two could-not-check receipts at HEAD a887298.
