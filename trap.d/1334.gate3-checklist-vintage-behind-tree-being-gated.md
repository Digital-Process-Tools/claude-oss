Gate 3 release audit (round 1, dispatch gate3-d0e2cdbcc2ca), config finding.

The installed release-auditor checklist was behind the tree it was gating: `agents/auditor.md` was
14,174 B installed (plugin cache pinned at 0.26.0) vs 14,402 B at HEAD; `skills/manager/phases/
findings.md` was 9,326 B installed vs 11,222 B at HEAD (the installed copy predated #1275's "Routing
a finding" section). checklist_skew.py against the resolved install (0.29.1, matching this repo's own
manifest version) read `state: matches`, but even the matched pair had one differing file --
`skills/manager/phases/accounting.md` -- because this repo is past v0.29.1 by 13 commits and the
plugin cache had not been refreshed to HEAD. Per release.md's own rule ("A matches payload carrying a
differs row is a config finding the release report must quote"), this is exactly that case: this repo
ships the definitions being audited, and the gate is running slightly older prose than the tree it
audits, for a self-hosted plugin repo where "installed" always lags "checked out" until an explicit
plugin-update step runs.

Not itself a defect to fix in code -- named so a future release remembers to resolve the plugin root
from a fresher install (or explicitly refresh the plugin cache) before gate 3 dispatches, rather than
trusting the session's ambient CLAUDE_PLUGIN_ROOT.
