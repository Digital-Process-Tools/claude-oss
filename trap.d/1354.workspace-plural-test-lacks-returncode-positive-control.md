Gate 3 release audit (v0.30.0, dispatch gate3-r2-68183c5a1a90, round 2, range
v0.29.1..HEAD), class misreports, non-blocking, companion to #1354.

tests/test_workspace_plugin_channel_arm_1307.py:155-173,
test_two_plugin_consumers_disarm_without_registering_oss_channel, asserts only
`not any("development-channels" in a for a in argv)` and `add_calls == []`.
Both are satisfied by the launcher crashing before it reaches the launch step
at all (see #1354) -- the test cannot distinguish "degraded gracefully" from
"aborted". It does not assert on returncode, unlike sibling tests in
tests/test_workspace_launcher.py (lines 395, 402, 652) which do. The module's
own docstring claims every "must not fire" case is paired with a "must fire"
positive control in the same fixture shape; for the plural case that pairing
does not exist -- the control it names covers the no-plugin-consumer path,
not this one. Fix: assert `done.returncode == 0` (or the documented degraded
exit) in this test once #1354 is fixed.
