Gate 3 release audit (v0.33.1..HEAD, round 2, token gate3-r2-bcc87282b35641f8) found
scripts/loop_cost_report.py:213-215 drops an assistant record whose timestamp is absent,
non-string or unparseable with no counter -- `when = _record_time(record)` then
`if when is None or when < since: continue` -- while the module's own docstring (lines
42-45) promises a malformed line is counted and reported, never skipped in silence.
Exercised: a transcript with a present, in-window timestamp gives
`state: measured, records: 1` (positive control); the identical fixture with the
timestamp key removed renders the same as a genuinely empty window --
`state: nothing-in-window, records: 0, malformed lines: 0, unreadable transcripts: 0`.
Fix: add a skipped_no_timestamp (or equivalent) counter to the payload alongside the
existing malformed/unreadable ones. Ranking: misreports, non-blocking.
