Gate-3 release audit (round 2, v0.37.1..HEAD) found: changelog.d/1628.added.worktree-reap.md states '.gitignore now covers both, plus the reviewer-spawn snapshot file' and that 'ignoring it here is what lets that warning mean something again' (referring to the git-worktrees false-positive 'uncommitted work' reading measured at 8-of-10). But scripts/scaffold.py's GITIGNORE template (lines 239-259) does not contain notes/, reports/ or oss-review-snapshot -- grep for all three in scaffold.py returns 0 results.

So the false-positive this delta's changelog claims to fix persists in every repo scaffolded by /oss:scaffold going forward (reaping itself is unaffected, since ARTIFACT_ALLOWLIST is a separate, independent list). Also, since .gitignore is a defaults-contract file ('created once when absent, then theirs forever'), even fixing the template would not reach an already-scaffolded repo -- the changelog's claimed reach is itself the finding, not merely a missed template edit.

Fix shape: add notes/, reports/ and the review-snapshot filename to scaffold.py's GITIGNORE template so newly-scaffolded repos get it, and note in the changelog/doc that already-scaffolded repos need the .gitignore addition applied by hand (or via a scaffold --apply pass) since it is a defaults file the plugin will never overwrite.
