Working #1389's picker-demotion follow-up, found that #1404 (the PR that built /oss:run and
removed the launcher's own prompt-picking logic) left several docs describing the OLD launcher
decision table -- the one that picked between /oss:tick and /oss:setup, or diverted to /oss:doctor
on an update. `bin/oss-workspace` no longer does any of that (every session now opens on /oss:run
unconditionally), but the docs describing it were not updated in #1404's own diff:

- docs/open-the-workspace.md:65 -- "**First prompt decision.** `.oss.json` present -> `/oss:tick`;
  absent -> `/oss:setup`." This whole doc appears to be a walkthrough of the pre-#1404 launcher and
  may need a substantial rewrite, not a one-line fix -- it also has its own decision table (lines
  ~145-152) mapping conditions to which prompt gets picked, which is exactly the logic #1392 removed.
- docs/install.md:111-118 -- "A genuine update selects `/oss:doctor` as the opening prompt instead
  of the ordinary `/oss:tick`/`/oss:setup` choice" -- same stale claim. I fixed the one paragraph in
  this file directly adjacent to my own change (the /oss:setup / /oss:scaffold mention) but left this
  second paragraph alone since it's a separate claim requiring its own verification against what
  bin/oss-workspace actually does post-#1392.
- docs/autonomy.md:49-50 mentioned `/oss:scaffold` directly in a passing example list (not a
  load-bearing claim, no test reads it) -- fixed in the same lane since it was a one-line, adjacent
  edit: now reads `/oss:run`, `/oss:doctor`, `/oss:tick`, `/oss:release`, `/oss:run scaffold`.

The remaining two are not load-bearing for anything I touched (no test currently reads their
content in a way that would catch a #1392-era staleness), which is presumably how they went
unnoticed. Filed here rather than fixed, since fixing docs/open-the-workspace.md properly looks
like its own change (verifying the current bin/oss-workspace behaviour line by line against the
doc's claims), not a rider on #1389's picker-demotion diff.
