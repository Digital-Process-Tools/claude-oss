Found by the v0.35.0 gate 3 release audit, round 1, ranked `misreports`.

`docs/pick-the-work.md` **was** edited in the v0.34.0..HEAD range (7e110e5f touched 4 lines), and
the same range invalidated five other passages in it that were not touched:

* `:129` still gives `--brief <brief-file>` as the canonical call shape.
* `:131` and `:133` still say the call "registers the lane" and writes "the lane registration",
  both retired by #1532.
* `:213-214` still cite `scripts/doctor_check_lane_patterns.py` and `scripts/lane_pattern_coverage.py`
  in the present tense; both were deleted in this range.
* `:249-250` still describe `lane_patterns_source` and a per-group `measured`/`inferred` adjacency,
  both removed from `select_issues.py`'s payload in this range.

This is user-facing documentation -- the only such file in the delta -- and a reader following it
types a call shape that no longer means what it says.

Settling fact, from the audit: `git grep -n 'lane_patterns_source\|adjacency' -- scripts/select_issues.py`
at HEAD returns nothing.
