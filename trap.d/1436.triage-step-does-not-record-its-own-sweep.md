Observed 2026-09-11 in /oss:run: the `oss:scheduler-step` spawn following `commands/run/triage.md`
completed a full sweep (13 labels applied, statusline marked stale) and reported back, and the very
next `next_action.py --root . --json` still ranked `triage` first as `due`, with `last_triage`
unchanged at 2026-09-09T18:26:54Z. The sweep was never written to the state file: nothing in
`commands/run/triage.md` tells the spawn to call `oss_state.py <state_file> --at <ts> --decision
"..." --triage-recorded <ts>`, so the scheduler would have re-spawned triage on every loop iteration
forever. The scheduler recorded it by hand this time. Cost: one extra rank call plus a manual
`oss_state.py` invocation and a first failed attempt (the `--at` requirement with `--decision` is not
in `--help`'s abbreviated usage line). Related to #1436's `--triage-recorded` finding: that one is
about `commands/tick.md`'s call shape; this one is about `commands/run/triage.md` making no call at
all. Confirmed by `oss_state.py --last-triage` before (2026-09-09) and after (2026-09-11) the manual
record.
