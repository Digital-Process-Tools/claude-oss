Release audit (gate 3, round 1, v0.34.0 candidate, dispatch token gate3-r1-0c628bb6fc093764)
found: .claude/jit-context/paths/00-manual/launcher-prompt-selection.md:26-33 still says "Four env
relays hand each computed reading forward, and are unset again right before exec... They are still
computed and still unset, unchanged, pending a decision (#1432)". #1474/0731470 removed three of the
four from bin/oss-workspace entirely -- the rule now fires on any session touching bin/oss-workspace
and sends it looking for exports that no longer exist.

Ranked `misreports`, non-blocking (findings.md's table) -- did not stop the v0.34.0 tag.

Settling fact: diff the rule's own claimed relay list against bin/oss-workspace's current exports
and update the rule (or retire the paragraph) to match what #1474 actually left in place.
