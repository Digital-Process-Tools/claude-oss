This tick (2026-09-09, sub-manager over Digital-Process-Tools/claude-oss), step 1's plugin-identity
check found this repo's own checkout well ahead of every copy of the plugin available to it:

- ${CLAUDE_PLUGIN_ROOT} (this session's injected pinned root): resolves to
  /Users/floriandavid/.claude/plugins/cache/dpt-plugins/oss/0.26.0
- resolved-install (plugin_update.py --print-resolved-root): 0.29.1
- the checkout itself (this repo, since claude-oss IS the oss plugin's own source): HEAD 1b02ecd,
  agent-report contract v12

Concretely this caused a real operational failure, not just a version-number mismatch: 0.26.0's
scripts/select_issues.py --board still has the pre-#1200 stdin-read bug (it tries to read a board
payload from stdin instead of fetching its own board), so it failed outright with
"stdin: not valid JSON". Recovered by running the checkout's own ./scripts/select_issues.py
directly instead, which worked correctly.

Two of this tick's own dispatched developer lanes (#1339, #1327) independently reported the same
symptom from the other direction: their own report_schema.py validation against the installed
cache copy (schema v9) came back UNVALIDATABLE against reports written to schema v12, while
validating fine against the checkout's own local scripts/report_schema.py.

Three readings of the same fact: whatever installs/updates ${CLAUDE_PLUGIN_ROOT} for this specific
session pinned a copy that is several releases behind both the resolved-install copy AND the
checkout being managed. Given this repo dogfeeds itself, a stale pinned root is not just cosmetic --
it makes tick-critical scripts (select_issues.py) silently regress to old, broken behavior. Worth
investigating whether the harness's plugin-root pinning mechanism is itself stale/broken for this
session, separately from the resolved-install vs. checkout gap that #1127/docs/release-currency.md
already tracks.
