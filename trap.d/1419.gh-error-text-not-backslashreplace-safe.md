Gate 3 release audit (v0.32.0, round 1, dispatch token gate3-r1-90bae15e5253) found (informational,
platform band): `scripts/cohort_freeze_record.py:378-395` (`_emit`) and `scripts/next_action.py:
712-743,822,852,856` both print `gh`/`git`-authored text (`reason`, stderr tails) through a bare
`print` with no `stream.reconfigure(errors="backslashreplace")`, unlike their siblings
`release_trigger.py:502` and `triage_trigger.py:286-290`, which do carry it.

`--json` is safe (`ensure_ascii` default) and `commands/run.md` calls these with `--json`, but
`commands/release.md`'s new Cohort block (~line 780) runs `cohort_freeze_record.py --execute`
*without* `--json` -- so a non-cp1252 character surfacing in a `gh` error on that path would kill
the receipt after the label writes already happened.

Coverage: `tests.yml:73` runs `windows-latest`, but the suites capture stdout rather than driving a
cp1252 console, so this is reasoned rather than observed.

Ranked `misreports` (a lost receipt on one rendering seam) -- non-blocking.
