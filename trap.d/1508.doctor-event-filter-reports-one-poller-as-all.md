Gate 3 release audit (v0.33.1..HEAD, round 1, token gate3-r1-3bf37232ab6d5c4f) found
scripts/doctor_check_event_filter.py:167 does `path = paths[0]` to inspect one gh-branch
poller's state file and reports OK over all of them. The glob at 152-154 can match several
state files (one per polled branch); only the first sorted one is read, and the OK line at
205-210 names that one ref with nothing saying others exist. Exercised: a second poller
subscribed to everything (`{"only": []}`) added beside a correctly-filtered one produces a
byte-identical OK line. Fix: iterate every matched state file rather than paths[0], or name
which ref was actually inspected in the OK line so the coverage gap is visible. Ranking:
misreports, non-blocking.
