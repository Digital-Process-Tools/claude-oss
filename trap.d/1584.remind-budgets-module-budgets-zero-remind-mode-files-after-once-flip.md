Gate-3 release audit (round 1, v0.37.1..HEAD) found: scripts/remind_budgets.py:1, 20-26 is titled "Size budgets for jit-context remind-mode bodies", premised on a remind rule paying its full body on every match. Commit 09155e9a (#1584 part one), in this same delta, flipped all six files remind_budgets.py budgets to mode: once -- visible as the -2 B re-baseline on each entry in that commit.

Neither change is wrong in isolation, but together they leave the module budgeting zero bodies of its own stated subject.

Exercised: mode census confirmed the six files remind_budgets.py budgets are all once; corpus-wide there are 44 no-mode-key rules, 24 once, 3 block, and 0 files still in remind mode.

Consequence: lines 20-26 of the module commission a follow-up sweep to widen the budget to "a remind body added later, or one of the remaining large siblings", naming four files -- a lane picking that up finds no remind bodies anywhere and cannot tell "we budget every remind body, there just are none" from "the check quietly stopped applying to anything". The byte budgets themselves still function and still pass at runtime, so nothing breaks; the guard is nominally on with an empty subject class and no positive control (no test asserts it still catches an oversized remind body, since none exist to author).

Fix shape: either retire remind_budgets.py's stated premise (there are no more remind-mode bodies to budget, so state that and repurpose or fold the module), or, if once-mode bodies still deserve their own byte budget for the same paid-on-every-match-until-dismissed reason, restate the module's docstring and follow-up sweep in terms of once rather than remind.
