Gate 3 release audit (v0.32.0, round 1, dispatch token gate3-r1-90bae15e5253) found:
`commands/tick.md:~130` (added by b81ef76e, #1386) instructs
`python3 …/oss_state.py <state_file> --triage-recorded "$(date …)"`, which cannot run as written.
`--triage-recorded` (#855, `scripts/oss_state.py:3534-3537`) is an attachment to `--decision`, and
the parser requires one of the mutually-exclusive mode flags first.

Exercised: `python3 scripts/oss_state.py /nonexistent-state.json --triage-recorded
2026-09-11T00:00:00Z` -> `error: one of the arguments --read --last --decision … is required`,
exit 2. Adding `--decision` makes it proceed, confirming the flag only works in that combination.

No other route records a sweep either -- grep for `triage-recorded` across `skills/ commands/
agents/ scripts/` finds only two prose mentions (`SKILL.md:524`, `accounting.md:296`);
`commands/run/triage.md` and `next_action._arm_route_source` (arms only the label-coverage receipt,
`next_action.py:687-709`) never write it. So a `/oss:run` that takes `triage` on `triage_trigger`'s
`due` re-ranks it `due` again on the next loop, per the paragraph's own stated consequence: "the
next `RELEASE: released` reads the same stale `--last-triage` and dispatches a triager again,
forever."

Composition: #855's flag semantics (pre-existing) x #1386's call site (new).

Ranked `misdirects` (the procedure names a next step that doesn't do what the caller was told it
does) -- non-blocking. Settling fact is the exit code above.
