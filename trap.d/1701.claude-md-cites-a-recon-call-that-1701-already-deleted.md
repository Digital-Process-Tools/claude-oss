Gate-3 audit (v0.42.0 round 1, range v0.41.0..HEAD). `CLAUDE.md`'s own
`agents/developer.md` ceiling-raise history (around line 364) says the
recon-call fix "pins the same call `skills/manager/phases/dispatch.md:62`
already carries". #1701 removed that dispatcher-side `Agent(subagent_type:
"oss:recon"...)` call from dispatch.md; a grep for
`Agent(subagent_type: "oss:recon"` in that file now returns nothing (the
recon call now lives only in the lane-owned prose at dispatch.md's own lines
45 and 550). The historical CLAUDE.md sentence is now describing a call site
that no longer exists at the line it names. What would settle it: reword that
sentence to name where the call actually lives today, or drop the specific
line reference.
