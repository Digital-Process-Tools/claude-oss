Gate-3 audit (v0.42.0 round 1, range v0.41.0..HEAD). `agents/scheduler-step.md`
(#1689) introduces a `PLUGIN-ROOT: could-not-run` handback line, but nothing
under `commands/`, `scripts/` or `skills/` greps for the string `PLUGIN-ROOT`
-- the scheduler reads a scheduler-step's handback as prose, and
`commands/run.md` names no such state for any of its own six sub-steps. The
line exists but has no documented reader that treats it as a stop. What would
settle it: `commands/run.md` naming `PLUGIN-ROOT: could-not-run` explicitly
beside the `setup` sub-step's own report shape.
