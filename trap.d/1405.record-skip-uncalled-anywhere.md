`next_action.record_skip()` (#1405) is only called from its own three unit tests today --
`commands/run.md` (which decides step-2 sequencing) does not call it, and no other script in
scripts/ references it, confirmed with a repo-wide grep. So its ValueError-on-nothing-to-skip
contract cannot crash a real tick yet -- it is genuinely dead code outside tests until something
wires a real caller.

Observed by the self-review auditor spawn on the #1405/#1406 diff. Worth a one-line pointer at
whichever call site eventually invokes it (most likely commands/run.md's own follow-up, see the
sibling trap fragment on that file's stale shape): the caller must check
`taken_source != candidates[0]["source"]` before calling `record_skip`, since that check is only
documented in this function's own docstring today and a caller written from commands/run.md's
prose alone, without reading the script, would not see it.
