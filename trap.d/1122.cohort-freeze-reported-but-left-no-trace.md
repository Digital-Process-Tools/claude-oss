A releaser reported `cohort-24 frozen at 37` in its final handback for v0.27.1. Nothing on the
repository records that freeze.

Observed 2026-09-07, after the v0.27.1 release had already completed:

    gh api repos/Digital-Process-Tools/claude-oss/labels/cohort-24 --jq '.description'
    cohort, frozen at a release

    gh api repos/Digital-Process-Tools/claude-oss/labels/cohort-23 --jq '.description'
    Open at the v0.27.0 tag, 2026-09-07. Frozen: nothing joins a cohort.

cohort-23 carries a dated, tag-specific description written at its freeze. cohort-24 still carries
the generic default string every cohort label is created with. A triage sweep run in the same
session read that same difference and concluded cohort-24 was the live, still-accumulating cohort --
the opposite of what the releaser reported, from the same evidence.

Corroborating: #1270 was filed at 2026-09-07T15:18:25Z, after the v0.27.1 tag at 15:01:15Z, and
carries cohort-24. If cohort-24 froze at that release, that label is a post-freeze application and a
violation. If it did not freeze, the label is correct and the handback is wrong. Both readings
cannot hold, and the honest state is that the label carries no evidence either way.

This is the second consecutive release whose cohort freeze cannot be independently confirmed.
CLAUDE.md already records the first: cohort-22, cited at 42 by the v0.26.0 marker, counted at 43 by
a fresh `issues.totalCount` read, with no `measured` freeze record in the state file. #1122 exists
because a marker cited a cohort count that had not been frozen yet; this is the adjacent failure --
a freeze *reported as done* with nothing written down that a later session could check.

What it cost: nothing yet, because it was caught by a contradiction between two agents in one
session rather than by a guard. That is the reason to log it -- the contradiction was luck. A single
agent reading only the handback would have carried `frozen at 37` forward into the next release
marker as fact, which is exactly the path CLAUDE.md's own "What is not proven yet" section warns
about and has now been on twice.

Possibly one rule with `scheduler.check-runs-absence-reads-green` and `1214.went-not-green-hides-a-real-red`:
in all three the artifact that would prove a check ran is absent, and the absence renders as a pass.
