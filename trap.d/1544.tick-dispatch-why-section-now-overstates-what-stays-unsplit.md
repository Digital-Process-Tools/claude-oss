Self-review on #1544 step 2 (agents/tick-review.md, feat/1544-step2) found a real but
out-of-scope staleness: `agents/tick-dispatch.md`'s own "Why this file exists" section says,
verbatim, "It is not the four-step split #1544 proposes. Steps 2-4 (wait+review, merge,
accounting+handback) stay `oss:sub-manager`'s, unsplit, in the same file, the same context, as
today." Step 2 (wait+review) is exactly what this diff split out into `agents/tick-review.md`, so
that sentence is now false for step 2 (still true for steps 3-4).

Not fixed in this diff on purpose: the maintainer's own brief for this lane explicitly said "do
not change step 1 (`agents/tick-dispatch.md`), which shipped in v0.35.0 and is working," with no
carve-out for a doc-only sentence. Flagged by two independent self-review spawns (Explore,
oss:auditor) rather than left silent.

Fix, when tick-dispatch.md is next touched: narrow the sentence to name only steps 3-4 ("Steps 3-4
(merge, accounting+handback) stay `oss:sub-manager`'s..."), or drop the claim entirely and point at
whichever file currently states the live split (this file's own header table, or a future
"what's split so far" line in agents/sub-manager.md). One line, no behavior change, budget-neutral
or near it (agents/tick-dispatch.md has 204 B of headroom per scripts/agent_budgets.py).
