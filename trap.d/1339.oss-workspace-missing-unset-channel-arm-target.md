Gate 3 audit round 1 (v0.30.0, dispatch gate3-6a7d9d04e7c7, range v0.29.1..HEAD), class misreports,
non-blocking.

bin/oss-workspace:2171-2177 states the invariant in its own words: a relay env var is "never meant
to reach the session itself", because a `claude mcp get` answer read at session-open would go on
answering an in-session /oss:doctor run for the rest of the session -- exactly the
reading-taken-once-treated-as-fresh-forever defect this repo is named after.

cffb3ec (#1307) added a third relay, `export OSS_WORKSPACE_CHANNEL_ARM_TARGET` at :1439, and did
not add it to the unset list at :2178-2180 (grep over both confirms the export exists and the two
unset lines do not name it). Consequence: for the life of a session opened through the `single`
branch, check_mcp_channel_registration short-circuits to OK on a launch-time reading for the whole
session. Composition defect: the export is #1307's, the unset list is #629's; neither diff alone is
wrong.
