Gate 3 release audit (v0.30.0, dispatch gate3-r2-68183c5a1a90, round 2, range
v0.29.1..HEAD), class misreports, non-blocking.

scripts/checklist_skew.py:218-226 -- _parse_effect_version returns matches[-1].
When the checklist-in-effect line names no explicit version, the last
version-shaped token is scraped out of the *file path* itself (e.g.
".../oss/0.29.1/agents/auditor.md") and returned as effect_version with no
marker distinguishing it from an explicitly-asserted version. "The auditor
asserted a version" and "we scraped a number out of a path" render
identically in the payload. The docstring anticipates the path-embeds-a-
version problem but only handles it for the explicit "version unknown" case.
Fix: have compare_effect's payload carry the provenance (asserted vs
path-scraped) alongside effect_version.
