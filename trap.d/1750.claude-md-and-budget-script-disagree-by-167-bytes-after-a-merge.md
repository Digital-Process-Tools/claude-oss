While merging `origin/main` into `fix/1750` (picking up the already-merged fix/1751 x fix/1753
history), I found `CLAUDE.md`'s own prose history for that prior merge cites `102,340 B` as the
re-measured post-merge size, while `scripts/claude_md_budget.py`'s own `BUDGETS["CLAUDE.md"]` tuple
for the identical merge reads `(102507, 103000)` -- a 167-byte disagreement between the two places
that are supposed to name the same fact.

Not reconciled here: neither commit's own diff is available to me to tell which number was measured
against which exact tree state (before or after some other small edit in the same merge commit), and
guessing which one is "right" risks introducing a third, equally unverifiable number. Both values are
left as committed by main, and this drift is recorded rather than silently overwritten.

Whoever looks at this next: re-derive both from a clean checkout of the merge commit that introduced
them (`git log --oneline -- CLAUDE.md scripts/claude_md_budget.py`, find the merge commit, `git show
<merge-commit>:CLAUDE.md | wc -c` at that exact tree) rather than trusting either committed number.
