Dispatched lane for #455 (PR #1291), and this same tick's own step 1 doctor.py run, both hit the
same SKEW independently: report_schema.py at the pinned CLAUDE_PLUGIN_ROOT (0.26.0) implements
schema version 9 and answers UNVALIDATABLE on a report written against version 10, because this
repo checkout itself (main) already declares 0.28.0 / contract version 10 and the installed plugin
cache has not caught up. Per agents/developer/report.md's own rule, the branch's own copy of
report_schema.py is authoritative when the repo being managed IS the plugin itself -- confirmed here
by comparing .claude-plugin/plugin.json's own name field in both copies ("oss" in each) -- so the
report validated fine against the branch's own script. Both a sub-manager and a developer lane
independently had to work around a stale CLAUDE_PLUGIN_ROOT this tick; worth checking whether
plugin_update.py's own currency check should surface this more loudly (or automatically) when
this repo is self-managing, rather than each agent independently discovering and reasoning around
it.
