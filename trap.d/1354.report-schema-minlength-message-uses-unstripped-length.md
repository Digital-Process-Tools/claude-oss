Gate 3 release audit (v0.30.0, dispatch gate3-r2-68183c5a1a90, round 2, range
v0.29.1..HEAD), class misreports, non-blocking.

scripts/report_schema.py:672-681 -- the minLength validator compares
len(value.strip()) against the minimum but the error message prints len(value).
A review.mechanism of 25 spaces renders "25 characters, shorter than the 20
minimum" -- a sentence that contradicts itself (25 > 20). A reader trusting the
receipt concludes the validator itself is broken rather than the field. Fix:
print the stripped length in the message, or name both lengths explicitly.
