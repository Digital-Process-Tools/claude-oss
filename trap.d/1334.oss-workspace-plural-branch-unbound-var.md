Gate 3 release audit (round 1, dispatch gate3-d0e2cdbcc2ca, range v0.29.1..HEAD), class B,
fails-to-preserve, non-blocking (impact is larger than the row suggests -- launcher becomes unusable
on this path -- maintainer's call whether to treat it as higher priority).

`bin/oss-workspace:1450-1466` (the `plural` branch). The branch comment asserts:

```
# `channel_ready` is already 0 here -- initialised once, above, before this
# whole if/elif/else (see that comment for why it is not repeated here).
```

False about its own file. `channel_ready=0` is at line 1496, after `else` on line 1466 -- inside the
third branch, not above the `if`. `channel_ready=1` at 1429 is inside `single`. In the `plural` path
the variable is never assigned, and `set -eu` is in effect (line 50), so the later
`if [ "$channel_ready" -eq 1 ]` aborts the shell.

Exercised (language behaviour): `sh -c 'set -eu; if [ "$channel_ready" -eq 1 ]; then echo armed; fi;
echo "reached exec"'` -> `sh: channel_ready: unbound variable`, exit 1, "reached exec" never printed.
The launcher dies before `exec claude` -- no session opens at all -- while the branch's own stderr
line says "Opening WITHOUT the channel flag". That is the outcome the file's own rule forbids: "A
session with no channel is worth more than no session."

Settles it: one-line fix, hoist `channel_ready=0` above the `if`.
