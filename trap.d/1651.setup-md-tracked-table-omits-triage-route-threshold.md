Release-auditor finding (round 2, v0.40.0..HEAD, dispatch token 831fb5269c44), ranked `misreports`,
non-blocking per skills/manager/phases/findings.md's table.

`commands/run/setup.md:164`'s split-ownership table enumerates what tracked `.oss.json` carries
and ends "..., milestones, release, curate_route_threshold" -- it omits `triage_route_threshold`,
which this same delta (#1676/#1677) made `--build` write unconditionally and which this same file
documents in prose 53 lines above, at `:111-116`. The delta added only that prose bullet and never
touched the table; the sibling key `curate_route_threshold` *is* in the table, added by #1616 for
exactly this reason.

Behaviour is correct: `--build` only prints to stdout (`oss_config.py:3871`), never writes
`.oss.json`, and `LOCAL_KEYS` (`clone`, `state_file`, `worktree_root`) does not include this key --
the ownership contract holds. Only the table's own documentation of it does not.

The one fact that would settle it: whether that table is meant to be exhaustive -- the
`curate_route_threshold` precedent says yes. Remedy: add `triage_route_threshold` to the table
beside `curate_route_threshold`.
