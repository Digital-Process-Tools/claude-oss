Gate 3 round 1 release audit (v0.32.0..HEAD, dispatch token gate3-r1-a3cefee77a71) found:
the same promoted TOOLS_MERGE_GATE rule (.claude/jit-context/tools/01-oss/merge-gate.md,
scripts/oss_rules.py) tells a session to run `git merge origin/main` before merging any PR "whose
base may have moved". skills/manager/phases/merge.md:75-76 (#1085) says green-and-mergeable means
merge with no pre-merge `git merge origin/main` "unless something other than staleness demands
one" -- the squash push run on the default branch is stated as the backstop instead. The promoted
rule fires at exactly the moment merge.md governs and says the opposite for the moved-base case;
the curate pass that promoted trap.d/1404...md apparently did not check it against the phase
file's own, contrary rule. Ranked `misdirects` -- can ship behind a trap.d fragment, no embargo.

What settles it: which of the two the loop actually means for a moved-base PR (pre-merge rebase,
or trust the squash's own push run). Whichever is chosen, the other document's wording needs
reconciling so they no longer disagree.
