Adjacent finding from the #1346 lane (statusline.py --mark-stale/--refresh --root-as-last-argv-
token IndexError fix): scripts/plugin_update.py:834 carries the identical unfixed pattern,
`argv[argv.index("--root") + 1] if "--root" in argv else os.getcwd()`, and would also raise
IndexError for --root as the very last token on the command line. Confirmed present by direct
read of that line. Different script/subsystem than #1346's own declared scope (statusline.py
only), so not fixed inside that lane -- would have been an undeclared, unreviewed rider. Worth
its own small fix using the same _arg_value-shaped helper #1346 introduced in statusline.py, if
one turns up owning that lane; non-blocking on its own.
