Gate 3 release audit (v0.32.0, round 1, dispatch token gate3-r1-90bae15e5253) found:
`skills/manager/phases/inbound.md:91-98` still says the statusline inbound count is "Left for a
lane that can measure it", while commit 66f8b256 (#1406), in this same delta, built exactly that
(`statusline._inbound_field`, `inbound_reading`, the render line). A phase file asserting a gap
that this delta closed in the same range.

Ranked `misreports` -- non-blocking. `inbound.md` is a new file (not present in the installed
0.31.0 checklist, `could-not-tell` in this gate's own skew payload), so nothing installed
contradicts it yet, but the stale sentence should be fixed before/alongside filing this trap so a
reader of the checked-out tree isn't told the opposite of what the tree does.
