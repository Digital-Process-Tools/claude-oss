Gate 3 release audit (v0.30.0, dispatch gate3-9ad91e037fb7, round 1, range v0.29.1..HEAD),
class misdirects, non-blocking.

scripts/doctor_check_statusline_unknowns.py:130 assembles a paste-able remedy command with
unescaped shell quoting:

    return 'python3 "{}" --refresh --root "{}"'.format(script, project_dir)

A `"` anywhere in project_dir terminates the quoting, so a maintainer who pastes the remedy runs
something other than a statusline refresh. Reachability is low and was checked rather than
assumed: lane_setup.py:867 derives worktree branches from the issue number via
derive_branch(config.get("branch_pattern"), issue), so a lane's own worktree path never carries
contributor-authored text. It takes the maintainer's own directory name containing a quote to
trigger. Fix: shlex.quote() both interpolated paths before formatting into the remedy string.
