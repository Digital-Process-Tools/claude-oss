Release-auditor finding (round 1, v0.40.0..HEAD, dispatch token 9230a81691d1), ranked `misreports`,
non-blocking per skills/manager/phases/findings.md's table.

`scripts/release_gate2.py:153-178` -- an absent `latest_review_comment_age_minutes` clears the gate,
while an absent `lane_active` correctly returns `could-not-tell`. Line 136 treats `None` as "never
established"; line 162 treats `None` as "no comment exists" and falls through to `clear`.

Observed: `[{"number":1,"review_decision":"NONE","lane_active":false}]` -> `DISPOSITION: clear`,
exit 0.

What a caller gets: a releaser that could not read review comments and simply omits the key clears
gate 2 on an unread signal, rather than reporting `could-not-tell`. Caller-facing prose in both
`skills/manager/phases/release.md` and `commands/release.md` says only "Feed it ... its latest
review comment's age" -- it never tells the caller to pass the literal `"unknown"`, which is
precisely what `--prs-json`'s own help text does spell out for the PR list itself ("never an empty
list for that case").

The one fact that would settle it: whether the age field is intended as required-with-`unknown`
(then this is a missing refusal, the same shape as the sibling fragment beside it) or genuinely
optional (then the two absences are two different facts sharing one spelling). Introduced by
PR #1684 (#1681).
