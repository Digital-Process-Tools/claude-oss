Gate 3 release audit (v0.33.1..HEAD, round 1, token gate3-r1-3bf37232ab6d5c4f) found
scripts/pr_green.py:472-478's new `status != "completed"` condition (#1480) does not cover
the run its own docstring cites as motivation, and does cover completed *failures* instead.
The docstring's evidence is a push-triggered tests run cancelled while still queued (run
34676539434, event=push, status=completed, conclusion=cancelled, jobs=0) -- but the
pre-existing event filter (event in _PR_RELEVANT_EVENTS) already excludes push-triggered
runs one line above, so the cited run never reaches the new status check at all. Exercised
with a stub: pull_request/queued/0 jobs -> uncovered=['tests'] (control can fail);
push/completed and push/queued both already -> [] via the event filter alone; the only
shape the new condition actually changes is pull_request/completed/0 jobs -> [], which now
also covers conclusion=failure and startup_failure since conclusion is never read.
Bounded: `_unresolved_runs` is only called when the rollup already reads GREEN and can only
downgrade GREEN->PENDING, so this cannot hide a rollup failure or turn red into green -- no
false green was observed. Ranking: misreports, non-blocking.
