Gate-3 audit (v0.42.0 round 1, range v0.41.0..HEAD). `agents/doctor.md:37`
writes the role marker (`agent_role.py --write doctor`) but names no `--clear`
call anywhere in the file, and `scripts/scaffold.py`/`commands/run/scaffold.md`
refuse `scaffold.py --apply` for up to 4 hours after any run that leaves a
`doctor` marker behind (the same single-slot marker lifetime #695/#1585
describe for sub-manager). So `/oss:doctor`, or `/oss:run` step 1, leaves a
live `doctor` marker; for up to 4 hours after, `/oss:run scaffold` refuses
`scaffold.py --apply` with "role 'doctor' may not run scaffold.py --apply"
even when the caller invoking scaffold is not the doctor. What would settle
it: the doctor spawn clearing its own marker at the end of its run, the same
way `tick_handback --clear-marker-root` already does for sub-manager.
