Dispatched lane for #455 (PR #1291) reported: tree_snapshot.py's compare read "mutated" once during
its own self-review round, caused by the before-snapshot JSON file itself (written inline inside the
worktree, per the jit-context rule's own advice for how to use it) still being present as an
untracked file at compare time. It was the lane's own artifact, not a reviewer write, and cost one
extra compare-and-diagnose round before the tree could be confirmed clean. Worth checking whether the
jit-context advice for where to write a before-snapshot should point outside the worktree, or whether
tree_snapshot.py's own compare should exclude a file matching its own snapshot naming convention.
