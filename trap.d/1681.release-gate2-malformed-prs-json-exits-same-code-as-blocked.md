Release-auditor finding (round 1, v0.40.0..HEAD, dispatch token 9230a81691d1), ranked `misreports`,
non-blocking per skills/manager/phases/findings.md's table.

`scripts/release_gate2.py:273` (and `:209`) -- an unreadable or malformed `--prs-json` file exits `1`,
the exact code the module's own docstring reserves for `blocked-by:N`, with no `DISPOSITION:` line at
all. `Path(args.prs_json).read_text()` is unguarded, and `decide()`'s comprehension calls `pr.get` on
each element with no type check.

Observed, not reasoned: `--prs-json /nonexistent/prs.json` -> FileNotFoundError, exit 1;
`--prs-json` naming `{"a":1}` -> AttributeError: 'str' object has no attribute 'get', exit 1.

What a caller gets: a releaser shell that reads only `$?` (as `commands/release.md` and
`skills/manager/phases/release.md` both instruct -- "exit codes, because a shell reads those and
never reads prose") records "gate 2 is blocked by a pull request" for a read that never happened.
This is `release_gate2.py`'s own stated defect class turned on itself: an absence produced by the
tool, read as an absence in the world.

The one fact that would settle it: whether any wrapper around this script reads stdout as well as
`$?`; nothing in the two files above says to. Introduced by PR #1684 (#1681).
