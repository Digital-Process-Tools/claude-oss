Schema/plugin skew, reconfirmed from a developer lane's own report validation during the
2026-09-09 tick (#1350): this repo IS the oss plugin's own source, and the installed plugin cache
this tick's ${CLAUDE_PLUGIN_ROOT} pointed at (.../oss/0.26.0, resolving further to
.../oss/0.29.1 via plugin_update.py --print-resolved-root) implements an older report-schema
version, while this checkout's own scripts/report_schema.py implements version 12. A lane's own
report_schema.py run against the installed cache copy answers UNVALIDATABLE; the same report
validates ok against this tree's own copy, which is authoritative per
agents/developer/report.md's self-hosting rule. Every ${CLAUDE_PLUGIN_ROOT}-rooted invocation
elsewhere in this loop (briefs, scripts, skill/phase prose) still runs out of the stale cached
copy unless a session deliberately overrides it, as this tick's sub-manager did after finding the
same SKEW independently at step 1 via doctor.py's own plugin-copy check. Worth a real fix (an
auto-update path that actually keeps pace with a self-hosting repo's own HEAD, or a session-start
check that surfaces the gap loudly) but the mitigation (read from the checkout when self-hosting)
is already documented and repeatable; not blocking on its own.
