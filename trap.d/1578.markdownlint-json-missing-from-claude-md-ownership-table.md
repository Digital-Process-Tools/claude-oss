#1578 added `.markdownlint.json` to `scripts/scaffold.py`'s `TEMPLATES` dict --
a `defaults`-tier file (created once when absent, then a decision somebody
made). `CLAUDE.md`'s "Three ownership contracts" table lists the `defaults`
row by name: `SECURITY.md`, `CLAUDE.md`, `.github/ISSUE_TEMPLATE/`,
`.gitignore`, `.supertool.json`. `.markdownlint.json` belongs in that list too
and is not there.

No test catches this. `tests/test_claude_md_ownership_table_1348.py` only
compares the `ours` row against `scaffold.OWNED` -- it has no equivalent
comparison for the `defaults` row against `scaffold.TEMPLATES` (minus the
files scaffold itself derives per-repo, like `CONTRIBUTING.md`/`CLAUDE.md`
which have their own renderers, or the fragments README whose path is
config-dependent). `commands/run/scaffold.md`'s own file table (checked by
`tests/test_content_invariants.py::test_scaffold_documents_every_file_it_writes`)
was updated in the same diff and does list it -- so the gap is specific to
`CLAUDE.md`'s ownership contracts section, not to documentation of scaffold's
output generally.

I did not edit `CLAUDE.md` myself: the developer brief's hand-curation rule
(`CLAUDE.md is curated by hand: the loop does not write it`) lists three
narrow exceptions and none of them cover a plain content-accuracy fix to the
ownership table -- the exception for a budgeted file only applies when a
lane's diff changes `agents/*.md`, `skills/manager/**` or `commands/tick.md`
such that a declared byte-count row goes stale, which is a different
mechanism than this one.

Possible fix for whoever curates this: add a comparison test in the same
shape as `test_claude_md_ownership_table_1348.py`, but against the
`defaults` row and `scaffold.TEMPLATES`'s hardcoded-content entries (the ones
using `lambda config: SOME_CONSTANT`, i.e. not `_render_claude_md`,
`_render_contributing_md`, or the fragments README) -- and separately, just
add `.markdownlint.json` to the `defaults` row by hand now.
