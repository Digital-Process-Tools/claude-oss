While re-baselining CLAUDE.md's own budget row for #1689/#1692/#1695 (the byte-budget table under
"This file has a size budget too"), found the table's declared baseline for CLAUDE.md itself
(72,547 B) did not match the true last-committed value the release marker history actually left
it at (71,848 B, per scripts/claude_md_budget.py's own inline history comment two paragraphs up in
the same file, describing a v0.41.0 release-commit fix that restored two dropped closing
paragraphs). The two numbers were already inconsistent before this lane touched either line --
confirmed with `git show c5a6e8c2:CLAUDE.md` style checks are not what I ran here, but the two
in-file sources of truth (the markdown table's own row vs. the inline history comment in
scripts/claude_md_budget.py) plainly disagreed at 74,508 B measurement time.

Cost: had to read both sources, notice the mismatch, and decide not to chase it -- reconciling
the exact provenance of the 72,547 vs 71,848 drift (which commit introduced it, whether it's the
markdown table falling behind an in-code comment update or vice versa) is a separate,
non-trivial archaeology task that would have doubled the size of an already self-referential
paragraph. Logged here instead of fixed, per the developer brief's own instruction for this
exact shape of drift ("that drift predates this change and is logged to trap.d/ rather than
reconciled here").

Confirmed by: `wc -c CLAUDE.md` immediately before touching either row, cross-referenced against
scripts/claude_md_budget.py's own comment history for the same file.
