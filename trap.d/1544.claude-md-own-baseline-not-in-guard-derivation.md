`lane_setup.py --lane CLAUDE.md --lane scripts/claude_md_budget.py ...` never named
`tests/test_baseline_matches_disk_1014.py` as a guard, even with `scripts/claude_md_budget.py`
explicitly passed as a lane file. That test checks CLAUDE.md's own single declared total-size
baseline (`scripts/claude_md_budget.py`'s `BUDGETS["CLAUDE.md"]`) against the file's real byte
count on disk -- a different, more coarse-grained check than the per-row budget table entries
`scripts/agent_budgets.py`/`scripts/skill_phases.py` cover, and CLAUDE.md's own governance rule
(the "third editing exception" for a lane that moves a budgeted file's row) requires updating both.

Cost: a pull request (#1544 step 2's PR #1569) pushed clean by every locally-run guard, then failed
4 of 7 CI legs on exactly this check -- `AssertionError: declared baseline disagrees with the
file's actual size on disk ... CLAUDE.md: declared 34266, actual 34985` -- caught only because the
maintainer's own review return read the pushed PR's checks rather than trusting the local run.

Confirmed by re-running `lane_setup.py --lane` with the same file set after the fact: the guard
list still does not include `tests/test_baseline_matches_disk_1014.py`. Whatever derives
`CROSS_CUTTING_GUARDS` from a touched-file set does not currently map "CLAUDE.md's table row
changed" to "run the disk-vs-declared-baseline check too" -- worth checking whether
`scripts/lane_setup.py`'s own guard derivation should add it whenever `CLAUDE.md` itself is a
named lane file, independent of which script also changed.
