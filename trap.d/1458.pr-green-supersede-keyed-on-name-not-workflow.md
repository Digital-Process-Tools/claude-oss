Release audit (gate 3, round 1, v0.34.0 candidate, dispatch token gate3-r1-0c628bb6fc093764)
found (reasoned, not observed): scripts/pr_green.py:172-205's _superseded_flags supersedes a failed
check by check *name* alone (`_leg_window` keys on row.get("name") or row.get("context")`), and
discards `workflowName`, which read_pr already has on the same row (:538). If two workflows on one
repo ever have a same-named job, a genuinely failed job in workflow A gets dropped from `failing`
the moment a same-named job in workflow B starts later on the same commit -- GitHub check-run names
are job names, not workflow-qualified. Not observed on any managed repo; the existing tests
(test_pr_green_superseded_1458.py:79-171) cover only single-workflow shapes, so nothing currently
exercises the two-workflow case either way.

Ranked `misreports`, non-blocking (findings.md's table) -- did not stop the v0.34.0 tag.

Settling fact: check whether supertool's own `_checks.github_superseded` (which this claims to
mirror) keys on (workflow, name) or on name alone -- if the former, this copy has diverged from what
it says it matches, and should key the same way.
