Gate 3 release audit (v0.32.0, round 1, dispatch token gate3-r1-90bae15e5253) found:
`scripts/triage_trigger.py:262-302` (`_load_config` + `main`) reads `.oss.json` raw with
`json.load` instead of going through `oss_config.load`, so it never merges `.oss.local.json` --
where `state_file` actually lives on every repo that has split its config the way
`commands/run/setup.md` instructs. It then reports "no state file configured, so the last triage
sweep could not be read", which is false on this repo: the file *is* configured, in the half it
did not read.

Exercised on this repo: `python3 scripts/triage_trigger.py --repo .` -> `triage-trigger: could not
tell / detail: no state file configured …`, exit 1 -- while `.oss.local.json` carries
`"state_file": ".max/claude-oss-watch.json"` and `oss_state.py … --last-triage` on that file
returns `recorded 2026-09-09T18:26:54Z`. `commands/tick.md:~118` prescribes exactly this call, with
no `--state-file`, so the scheduler-level trigger (#1386) reads `could-not-tell` on every release on
a repo with a split config, and the file tells the scheduler to never proceed on that reading.

Composition: #608's split config layout (pre-existing) x b81ef76e's raw loader added for #1386.
`next_action.py` is unaffected -- it uses `oss_config.load` correctly and evaluated `triage` as
not-due on this same repo in the same audit.

Ranked `misreports` (rendering/loader seam) -- non-blocking, one-call fix: route
`triage_trigger.py`'s config read through `oss_config.load` instead of a raw `json.load`.
