Gate 3 audit round 1 (v0.30.0, dispatch gate3-6a7d9d04e7c7, range v0.29.1..HEAD), class misreports,
non-blocking.

211fb6e (#1302) added trap.d/README.md to scaffold.py's OWNED set, replaced wholesale on every
/oss:scaffold run (commands/scaffold.md and tests/test_owned_files.py both updated). CLAUDE.md's
"Three ownership contracts" table -- loaded whole by every session, the canonical statement of what
this plugin may write into another repo -- still lists "ours" as only .oss/,
.github/workflows/oss-changelog.yml, .claude/jit-context/*/01-oss/, which places trap.d/ under
"yours: never read, never written" by omission.

No test compares this CLAUDE.md table against scaffold.OWNED the way test_claude_md_budget_
table_709.py and test_claude_md_phase_budget_table_725.py already compare the budget tables to
their source modules, so the drift is structurally invisible. Note: CLAUDE.md's own "Working here"
rule forbids a lane from editing this file except for budget rows, so this gap is a side effect of
that rule rather than an oversight by any one lane -- an argument for a guard test, not for a lane
silently fixing the prose.
