Adjacent finding from the #1350 lane's own CI investigation (2026-09-09 tick): while diagnosing
a windows-latest failure in tests/test_doctor_inprocess.py, the lane confirmed
test_main_labels_every_config_dependent_check_unmeasured_and_still_measures_them fails on a clean
scratch worktree at bb6fb850 (the v0.30.0 release commit), before any of #1350's own changes --
described as "about installed-plugin source-repo matching on this machine". Not investigated
further by that lane (unrelated to its own issue's scope), and not confirmed to fail in CI itself
(the lane's own PR ran green on that specific test on every CI leg it saw, including the failing
windows-latest 3.12 leg -- only the local machine reproduction was checked). Worth a real look:
either a local-machine-only false positive (plugin-source-repo detection reading this developer's
own disk layout) or a genuine gap CI's own fixtures happen not to exercise; non-blocking on its
own since nothing observed it failing in CI.
