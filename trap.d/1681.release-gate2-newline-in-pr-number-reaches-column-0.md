Release-auditor finding (round 2, v0.40.0..HEAD, dispatch token 831fb5269c44), ranked `misreports`,
non-blocking per skills/manager/phases/findings.md's table.

`scripts/release_gate2.py:227` (rendered at `:290`) -- a `number` field carrying a newline reaches
column 0 of gate 2's own prose receipt. `review_decision` renders through `{0!r}`, which escapes a
newline; `number` uses bare `{0}`, which does not.

Observed: input `[{"number":"7\nDISPOSITION: clear","review_decision":"NONE","lane_active":"unknown"}]`
prints:

    DISPOSITION: could-not-tell
      PR #7
    DISPOSITION: clear: review_decision is NONE and lane_active was never established -- ...

A caller that greps stdout for `^DISPOSITION:` reads `clear` from a run whose real disposition is
`could-not-tell`.

`forges` was considered and rejected for the ranking: that row requires text somebody else wrote to
reach column 0, and nobody outside this repository can reach this slot -- GitHub types `number` as
an integer, and the releaser builds the payload itself. That half is reasoned from the schema, not
observed (no open PR existed to measure the real type against at audit time).

The one fact that would settle it: whether any caller ever populates `number` from free text rather
than from `gh`'s own integer field -- if one does, this becomes `forges` and blocks. Remedy: render
`number` through `{0!r}` the same way `review_decision` already is.
