During this lane, `supertool 'read:scripts/release_trigger.py:...'` and
`supertool 'grep:_TIMEOUT:scripts/release_trigger.py:...'` both returned the file's pristine,
pre-#1566 content (539 lines, no `_stale_local_head` function at all) on calls made several `edit:@-`
operations after those edits had already applied -- each `edit:@-` call itself returned a diff
showing the new content landing correctly, immediately before the stale read/grep. `wc -l
scripts/release_trigger.py` (598, then 612 lines across the two edits) and a direct
`python3 -c "print(open('scripts/release_trigger.py').read())"` both showed the real, current,
edited content at the same moments the `read`/`grep` ops were returning the old one.

Observed twice in one session, both times on the same file, both times several edits deep. Not
reproduced deliberately -- no attempt was made to isolate whether it is a path-keyed cache, a
line-count-keyed index, or something else, since the workaround (fall back to a raw Python read to
confirm ground truth) was cheap and available immediately. Cost: two ~230-line reads whose content
had to be independently re-verified before being trusted, plus the time spent momentarily believing
an edit had silently failed or been reverted.

Confirmed by: `python3 -c` reading the same file at the same point in the session returned the
current content; `git status --short` and `git diff --stat` both agreed the edits were staged as
expected; a subsequent `grep` for the newly-added string (`fetched, _, fetch_detail`) also came back
empty even though a raw Python substring search found it present, ruling out a grep-pattern mistake
on my part.
