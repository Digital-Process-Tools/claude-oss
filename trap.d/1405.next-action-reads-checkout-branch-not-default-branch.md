Observed 2026-09-11 in /oss:run: after curate had committed and pushed 3db5581 to main (35
fragments removed), `next_action.py --root . --json` ranked `curate` as `due` again with "trap.d/
has 38 fragment(s)". The checkout at `.` had meanwhile been switched by the maintainer's own live
session to `fix/1423`, a branch cut before the curate commit, so the count came from that branch's
stale trap.d/, not from the default branch. Two things cost time here: (1) the receipt reads the
working tree of whatever branch happens to be checked out rather than `origin/<default_branch>`,
so a shared checkout gives a wrong answer whenever someone else is on a feature branch; (2)
`--record-skip <taken_source>` refuses anything not in the call's own ranked sources
(`['curate']` here), so a deliberate fall-through to dispatch could only be recorded as a free-text
`oss_state.py --decision`, invisible to the repeat-suppression receipt. Confirmed with
`git merge-base --is-ancestor 3db5581 HEAD` (not an ancestor) and `git log origin/main -1`
(ed13244, which does contain it). The scheduler proceeded to dispatch without re-checking out main
under the maintainer.
