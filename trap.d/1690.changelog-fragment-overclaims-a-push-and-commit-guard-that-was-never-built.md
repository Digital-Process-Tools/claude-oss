Gate-3 audit (v0.41.1 round 2, range v0.41.0..HEAD). `changelog.d/1690.fixed.md`
says the release carries "a code-level guard against a doctor spawn applying,
committing and pushing unbidden". `git grep` over `scripts/`, `hooks/` and
`bin/` finds `agent_role.DOCTOR` used only by `role_forbids_scaffold_apply` --
nothing refuses a commit or a push under the `doctor` marker, and
`role_forbids_release` still refuses `sub-manager` only. #1690's own ask (that
`release_publish.py`, `push_bypass.py` and a pre-push guard refuse under
`doctor`) was never implemented, and #1690 is already closed. Because of the
sibling finding filed as issue #1719 (the doctor's own brief always passes
`--i-was-asked`), even the one guard that does exist is not effective either.
What a caller gets: release notes promising every installed user a push guard
that does not exist. What would settle it: one grep for a doctor-role check
on any push path (there is none today), and either building the missing
guards or correcting the changelog fragment's claim.
