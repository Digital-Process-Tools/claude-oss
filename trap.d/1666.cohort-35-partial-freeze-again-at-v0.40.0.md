Cutting v0.40.0, `cohort_freeze_record.py --tag v0.40.0 --cohort 35 --execute` again reported
`partial`: `cutoff_scan=5` against `label_filter=34`, the same shape as the v0.39.0 attempt logged
in `trap.d/1666.cohort-35-label-accumulated-across-repeated-partial-freeze-attempts.md` (which is
still open, unpromoted). This is now the fourth-or-later partial attempt on `cohort-35` across
successive tags (v0.37.0, v0.37.1, v0.38.0, v0.39.0, now v0.40.0), and the label write happened
again before the disagreement was discovered (issues #1667, #1670 added to the `cohort-35` label
this run). The citation in `CLAUDE.md`'s marker correctly remained `cohort-34`, per the
citation-order rule -- `cohort_citation_order.py` reported `ok -- cohort-34 was already frozen`
both before drafting the marker and again before committing. Not fixed here: the underlying
mechanism issue is already tracked in the fragment named above; this is a fresh occurrence, not a
new class of defect, logged so a future curate pass can see the recurrence count. Ranking-table
row `misreports` (non-blocking; the tag and release were already complete before this freeze step
ran, and the cohort-freeze step runs strictly after the tag by design).
