Gate 3 audit round 1 (v0.30.0, dispatch gate3-6a7d9d04e7c7, range v0.29.1..HEAD), class misreports,
non-blocking.

scripts/statusline.py:2932-2947 -- `--mark-stale` returns 0 and prints nothing whether the board
was actually marked or no repo resolved. commands/triage.md (added in this same delta) makes
--mark-stale a mandatory end-of-sweep step for an orchestrating session that reads an exit code, and
the command file's own prose acknowledges the silent no-op -- prose is not a receipt.

Secondary, same block, :2942: `root = argv[argv.index("--root") + 1]` raises IndexError when
--root is the last argument on the command line. This is the same shape as the pre-existing --refresh
arm at :2950; the pattern is not new but this instance is.
