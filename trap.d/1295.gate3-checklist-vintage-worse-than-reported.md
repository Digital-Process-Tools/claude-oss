Release v0.29.0 gate 3, round 1 (dispatch token 3e96e2b2f6e6d380): checklist_skew.py reported
installed_version=0.27.1 vs repo_version=0.28.0, state=differs. But the auditor spawn actually
loaded its own definition from .../dpt-plugins/oss/0.26.0/ (per its own report: "checklist in
effect: .../0.26.0/agents/auditor.md version 0.26.0"), not from the 0.27.1 that plugin_update.py's
--print-resolved-root reported as installed. So the real skew the audit ran under was one step
worse than checklist_skew.py's own payload said: two minor versions old, not one. Concretely,
0.26.0's skills/manager/phases/findings.md is 9,326 B with no "Routing a finding" section (#1275);
the checkout's is 11,222 B and has one.

The gap: checklist_skew.py measures the resolved-install root via plugin_update.py, but the actual
Agent(subagent_type: "oss:release-auditor", ...) spawn resolves its system prompt through the
harness's own plugin registration, which is a separate mechanism that can point somewhere else
entirely. Nothing in the release gate compares "which plugin version checklist_skew.py measured"
against "which plugin version the spawned agent's own system prompt actually came from" -- they can
and did disagree in this run. trap.d/1291.report-schema-skew-installed-vs-checkout.md (same
release's own delta) already records a sub-manager and a developer lane hitting the same class of
skew independently in the same tick -- this is a third, gate-3-specific instance of it.

Worth a mechanism that reads the spawned agent's own loaded definition version (if that's
observable at all) rather than trusting plugin_update.py's resolved-root guess to match it.
