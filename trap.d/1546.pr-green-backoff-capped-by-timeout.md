Found by the v0.35.0 gate 3 release audit, round 1, ranked `misreports`.

`scripts/pr_green.py:776-790`: #1492's rate-limit backoff is capped to the base interval whenever
`--timeout` is set -- `wait_interval = min(wait_interval, interval)`, unconditional inside
`if timeout is not None`. `agents/sub-manager.md:203` prescribes exactly
`pr_green.py NUM --wait --timeout N`, so on the loop's own sanctioned call the backoff can never
widen anything, while `--interval`'s own help text (line 915) tells that caller it "widens
automatically when GitHub's shared REST rate limit runs low".

`skills/manager/phases/ci-green.md:17` shows the shape **without** `--timeout`, so the two loop
documents disagree about which invocation is canonical. Both commits are individually clean; the
join is the defect.

Secondary, and relevant to #1499's own subject: with `--timeout` set the reader still spends one
`gh api rate_limit` subprocess before every sleep, whose result is then discarded. A per-poll cost
in the release whose theme is cutting cost.

What would settle it: which of the two documented shapes a tick actually types.
