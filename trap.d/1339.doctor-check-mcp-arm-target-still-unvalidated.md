Gate 3 audit round 1 (v0.30.0, dispatch gate3-6a7d9d04e7c7, range v0.29.1..HEAD), class misreports,
non-blocking. Same mechanism logged in the prior round at
trap.d/1334.doctor-check-mcp-arm-target-unvalidated.md; re-derived, still present.

scripts/doctor_check_mcp_channel_registration.py:246-254: `arm_target =
env.get("OSS_WORKSPACE_CHANNEL_ARM_TARGET", "")`; any non-empty value differing from `label`
converts a genuine not-registered finding into OK and returns. Nothing verifies the named server
actually resolves via `claude mcp get`, and nothing distinguishes "the launcher set this in the
branch that owns it" from an inherited or stale export. A diagnostic silenceable by an unvalidated
environment variable is nominally on and effectively off.
