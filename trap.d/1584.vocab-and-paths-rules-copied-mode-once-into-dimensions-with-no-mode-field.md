Gate-3 release audit (round 2, v0.37.1..HEAD) found: commit 09155e9a (#1584) flipped 19 tools rules to mode: once, entirely correct there. Three weeks later, 3640a43f (#1612) added three vocabulary rules and 042e6e7a (#1632) added one paths rule, each copying mode: once into a dimension that has no mode field at all -- doctor.py's own documented index arities (tools = tool/match/filename/mode/require/forbid/requires; paths = match/filename; vocabulary = keyword/filename/verdict) show neither paths nor vocabulary carries a mode column, before or after this delta.

The claude-jit-context 0.10.0 source itself (scripts/common.sh) says the paths and vocabulary dimensions are deduped per session regardless of any mode field, and once semantics (per-reader dedup) apply only to tools. Since a spawned agent inherits its parent's session id, these four entries (three vocabulary + one paths) reach at most one agent per whole /oss:run session -- exactly the problem once-mode exists to fix, and the opposite of what oss_rules.py's own comment and changelog.d/1607.added.vocabulary-entries.md claim for them.

Affected: .claude/jit-context/vocabulary/01-oss/{dev-lanes,oss-loop,sub-manager-vocab}.md and .claude/jit-context/paths/00-manual/regex-against-stringified-path-assumes-posix-separator.md, plus the generator in scripts/oss_rules.py (lines ~500-506, 511, 543, 577) that writes mode: once for all four.

Fix shape: strip the mode: field from paths/vocabulary rule generation (there being no mode column to honour), and correct the stated rationale in oss_rules.py and the changelog fragment to stop claiming per-reader dedup for a dimension that has never supported it.
