Gate 3 release audit (round 1, dispatch gate3-d0e2cdbcc2ca, range v0.29.1..HEAD), class A, misreports,
non-blocking.

`scripts/doctor_check_statusline_unknowns.py:377-380`:

```python
if statusline is None or not repo:
    cache = None
```

A `.oss.json` with no `repo` key collapses into the same `cache = None` as "the cache has never been
written". `channel_cause` then returns `not-asked`, and `_report_channel` prints the WARN "nobody has
taken a `channel:health` reading yet ... {remedy}" with `--refresh` as the remedy. `--refresh` also
resolves nothing without `repo`, so the WARN can never be cleared by the command it names -- which
this repo's own doctor-check-contract calls a defect in the check. This is the exact collapse the
module's own docstring says it exists to prevent, one field over.

Settles it: add a `repo`-missing arm distinct from `not-asked`, or show that `repo` is structurally
guaranteed present by the time this check runs.
