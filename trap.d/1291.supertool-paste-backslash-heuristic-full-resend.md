Dispatched lane for #455 (PR #1291) reported: supertool's `paste` op refused the PR-body payload's
first draft over an even-backslash-run heuristic on JSON escape sequences written inside a TOML
literal string -- a correct catch (the draft did have doubled backslashes meant as single JSON
escapes), but the remedy the refusal offers is a full resend of the whole `content` field rather than
a targeted correction, which cost one extra round-trip on a roughly 4KB payload. Worth checking
whether the refusal message could point at the specific offending line/offset rather than only the
field as a whole, so a resend can be a small fix instead of retyping everything.
