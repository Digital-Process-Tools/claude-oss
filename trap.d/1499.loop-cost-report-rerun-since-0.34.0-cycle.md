Item 7 of #1499's checklist asks for a second `loop_cost_report.py` run over an
overnight period, to compare against whatever baseline motivated the issue, plus a
decision about enforcement. This fragment is that re-run's own result and a note
about what could not be settled from inside this lane.

Command run from inside a worktree of this repo, 2026-09-14, against real
transcript data (not scratch/synthetic data):

    python3 scripts/loop_cost_report.py --since 2026-09-12T00:00:00Z \
        --repo-dir=-Users-floriandavid-Documents-claude-oss

STATE: measured. records: 10,188  malformed: 0  unreadable: 0. Window covers the
gate3 v0.33.1 rounds and the v0.34.0 release cycle (curate pass, the two gate3
routing PRs #1513/#1514, and this very lane's own recon+dispatch).

Per-kind share of the 1,816,881,210 context tokens sent in the window:

  kind             agents  records       context sent      max ctx  share
  main-session         11    1,296        335,468,050      593,088   18%
  sub-manager           6    1,254        298,013,748      497,512   16%
  releaser              2      483         87,208,948      278,078    5%
  scheduler-step        3      403         68,778,409      306,361    4%
  developer            30    4,477        813,718,325      502,878   45%
  audit-review         35    1,493         97,243,068      206,732    5%
  other                14      782        116,450,662      369,891    6%

Band distribution: <100k 10%, 100-200k 30%, 200-300k 30%, 300-400k 20%, >400k 10%.
Six transcripts crossed the 400,000-token DEFAULT_DEFECT_AT threshold in this window
(one main-session at 593,088; a developer lane at 502,878; three sub-manager
spawns at 497,512 / 415,706 / 407,606; one main-session /oss:run invocation at
419,464). `developer` remains the largest single share of total spend at 45%,
consistent with #491's own framing that the developer lane is the product and
where architectural changes should earn their keep.

What this fragment does NOT settle, because it is out of this lane's scope
(decided by the dispatching brief, not re-litigated here):

- No prior loop_cost_report.py run exists anywhere in this repo's history to diff
  against -- CHANGELOG.md documents the script's creation (item 1) but no earlier
  invocation's numbers. So this is a fresh baseline, not a before/after comparison.
  A second run after this cycle's own fixes (items 1-4, 6) have had more time to
  bite -- especially the no-hand-poll jit rule and the pr_exclude_events default,
  both landed inside this same 2026-09-12+ window -- would be the natural next
  comparison point, once a full multi-day cycle has run under them.
- No enforcement decision (e.g. failing a lane that crosses DEFAULT_DEFECT_AT, or
  routing it automatically to a trap.d finding) is made here. `skills/manager/
  phases/handback.md` already tells a sub-manager to read a developer report's
  `cost.over_threshold` and route it as a trap.d/ finding (#1499, landed earlier in
  this same umbrella issue) -- that is the enforcement mechanism that exists today.
  Whether a harder gate (refusing to merge, or auto-splitting a lane) is warranted
  is a maintainer call, not something a single re-run settles.
- `developer` at 45% share and `sub-manager` at 16% is the same shape item 5
  (the deferred, explicitly-out-of-scope "split the sub-manager per tick
  step" rearchitecture) would address on the sub-manager side; this number
  is data for that future maintainer decision, not an argument this
  fragment is making on its own.
