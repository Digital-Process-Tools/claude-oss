Gate 3 release audit (v0.33.1..HEAD, round 2, token gate3-r2-bcc87282b35641f8) found
scripts/lane_setup_brief_schema.py:274-292's check_recon does
`if "recon" not in lowered`, a bare substring with no word boundary, so a brief carrying
no recon material at all can still read ok / "nine elements found". Exercised:
"Please reconsider the approach before starting." -> state: found;
"We should reconstruct the fixture." -> state: found;
"No orientation material at all here." -> state: missing (positive control, the check can
fail). Fix: anchor on a word boundary or the brief's own recon section heading rather than
a bare substring. Ranking: misreports, non-blocking.
