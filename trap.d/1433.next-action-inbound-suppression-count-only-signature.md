Gate 3 release audit (v0.33.1..HEAD, round 2, token gate3-r2-bcc87282b35641f8) found
scripts/next_action.py:380-397's new inbound repeat-suppression signature
("unruled:{0},unreviewed:{1}") is counts-only, the same shape _curate_candidate and
_triage_candidate already use and predate this change. Consequence for the new inbound
source specifically: once a reading has been committed to at a given count, a different
outside issue arriving while an old one is still ruled leaves the count unchanged, so the
candidate reports not-due with "unchanged since the last time this reading was routed" --
false about the reading's actual content, since inbound items are the one source that
comes from outside contributors. Exercised: first look due; after arm=True, same count ->
not-due; count moved to 2 -> due (positive control, suppression can lift). Fix: a
signature carrying issue/PR identity rather than a bare count, at least for the inbound
source. Ranking: misreports, non-blocking.
