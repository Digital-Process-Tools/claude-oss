Gate 3 release audit (round 1, dispatch gate3-d0e2cdbcc2ca, range v0.29.1..HEAD), class B, misreports,
non-blocking.

`scripts/doctor_check_mcp_channel_registration.py:246-254`:

```python
arm_target = env.get("OSS_WORKSPACE_CHANNEL_ARM_TARGET", "")
if state == "not-registered" and arm_target and arm_target != label:
    doctor.report("OK", ...)
    return
```

Any value in the environment turns a genuine `not-registered` finding into an OK. Nothing checks that
the named server actually exists, and nothing distinguishes "the launcher set this in the branch that
owns it" from "inherited from an unrelated parent shell or a stale export". A diagnostic that can be
silenced by an unvalidated environment variable is a guard that is nominally on and effectively off.

Settles it: verify `arm_target` resolves (`claude mcp get`) before rendering OK, or report a third
state naming the relay rather than a clean pass.
