Issue 1044, dispatched into a lane alongside 1520 and 1522 (2026-09-15). The issue's own
first-half claim (a `no-adjacent` reason accepted with no truthfulness check) was already
fixed by 918/921, confirmed still live at scripts/select_issues_rank.py:440
(`check_lane(..., adjacent=...)`) and wired through scripts/oss_state.py around line 1786.
The issue's own 2026-09-05 comment already says as much and narrows the issue to its
second half: "rank-then-hunt" -- the loop selects the best single issue and only then
searches for adjacent companions, rather than selecting the best bundle.

That second half is not dispatchable as a mechanical fix. The issue's own body says, in as
many words: "Not decided here: Bundle-first selection, a candidate check on no-adjacent,
dropping adjacency in favour of disjointness-against-running-lanes, or measuring whether a
non-adjacent bundle is actually cheaper first." The maintainer's own comment reiterates:
"a genuine design question, not a mechanical bug, and needs a maintainer decision on the
selection algorithm's shape before it is dispatchable as a diff."

A developer lane spawned against this issue has no authority to make that selection-shape
decision, and picking one unilaterally would be exactly the kind of unbriefed design call
the developer brief tells a lane to push back on rather than take. Cost: one recon pass
plus one issue read established this before any code was touched -- cheap, but worth a
fragment so a future dispatch does not re-derive the same "not dispatchable without a
maintainer decision" conclusion from scratch. If a maintainer sets the selection-shape
decision explicitly (bundle-first vs. top-issue-then-search, whether adjacency is even the
right proxy for the fixed-overhead saving 799 measured), then this becomes a real diff.
