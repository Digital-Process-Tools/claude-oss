Gate-3 audit (v0.42.0 round 1, range v0.41.0..HEAD). `agents/triager.md:44`
(#1695) hardcodes the priority floor as the literal label `priority-low`, but
`labels.priority` is a per-repo, discovered spelling (this same file's own
line 25 says "do not invent one ... say so and stop", and
`skills/manager/SKILL.md` names a sibling repo that spells it
`priority:high`). A repo whose own `.oss.json` declares a different spelling
gets a floor instruction naming a label that is not there. What would settle
it: say "the lowest of `labels.priority`, in declared order" rather than a
literal string.
