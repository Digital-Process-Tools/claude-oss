Gate 3 release audit (round 1, dispatch gate3-d0e2cdbcc2ca, range v0.29.1..HEAD), class A, misreports,
non-blocking.

`scripts/statusline.py:2858-2872` (`main`, `--mark-stale`):

```python
repo = repo_config(root).get("repo")
if repo:
    mark_board_stale(repo)
return 0
```

Exit 0 and no output whether the board was marked or no repo resolved. `commands/triage.md` (added
in this same delta) makes this a mandatory end-of-sweep step and acknowledges the silent no-op in
prose -- but the caller is an orchestrating session reading an exit code, and 0 is the same answer
either way.

Secondary, same block: `root = argv[argv.index("--root") + 1]` raises `IndexError` when `--root` is
the final argument.

Settles it: print a one-line receipt naming which of the two happened (marked / not marked, and why).
