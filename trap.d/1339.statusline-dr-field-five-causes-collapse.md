Gate 3 audit round 1 (v0.30.0, dispatch gate3-6a7d9d04e7c7, range v0.29.1..HEAD), class misreports,
non-blocking.

scripts/statusline.py:1338, :2460-2512 append a `dr` field on every render. `_doctor_reading`
(scripts/doctor_check_statusline_unknowns.py) returns bare `None` for five distinct causes: no
doctor.py located, spawn failure, DOCTOR_TIMEOUT expiry, non-zero exit, and no VERDICT: line in the
output. All five collapse onto the same `dr?` in the rendered status line.

This is a composition defect assembled from two individually clean commits: c8d4c20 (#1311) added
check_statusline_unknowns to "explain the cause of every `?` statusline.py can render", and 062c5de
(#1314), landed later in the same delta, added a third `?`-capable field (`dr`) that it does not
cover. Confirmed by grep over doctor_check_statusline_unknowns.py for doctor_state/doctor_verdict/
"doctor marker"/dr\? -- 0 results.

Fix would add an arm in doctor_check_statusline_unknowns.py naming which of the five produced the
`?`, or state deliberately that `dr?` is unexplained.
