Gate 3 round 1 audit for v0.29.1 (dispatch token gate3-v0291-r1-a685778e7634) found that tagging
the bare fix commit (a887298, PR #1300's merge) directly as v0.29.1 -- without a proper release
commit bumping the four version_sites, folding a changelog.d fragment, and updating CLAUDE.md's
own marker -- would ship an artifact whose own manifest still says 0.28.0/0.29.0. Every prior tag
in this repo agrees with its own manifest (checked v0.27.0, v0.27.1, v0.28.0, v0.29.0), and
commands/release.md's own procedure produces a release commit before the tag moves. Followed
correctly this time: the v0.29.1 release commit bumps all four version_sites, folds
changelog.d/1299.fixed.md, and rewrites the CLAUDE.md marker to name v0.29.1 before the tag is
written, so this finding did not recur in this release -- recorded so the next patch release
(fixing forward a bad tag, the v0.27.1/v0.29.1 shape) does not skip the release-commit step under
time pressure and tag the bare fix commit directly.

Confirmed by: comparing .claude-plugin/plugin.json's version field against every prior tag's own
manifest at that commit.
