The v0.28.0 release commit (dd2353e, prose-only: CLAUDE.md marker, version bumps, changelog fold)
went RED on `pytest (windows-latest, 3.12)` with the exact #1228 "site 3" signature already
documented on that issue's thread: `FileNotFoundError: ... skills\manager\phases\undeclared-
control.md` from tests/test_skill_phase_split.py's `test_a_phase_file_on_disk_that_nobody_
budgeted_is_reported` control file, raced away by a sibling xdist worker walking/cleaning that
same tracked directory. #1228 was closed by PR #1249, which fixed sites 3 and 4 per that issue's
own description -- this is either a recurrence the fix did not fully close, or a related but
distinct race in the same directory. Per #1228's own established precedent across five prior
firings, this is the race and not a content defect (the release commit's diff touches no test
infrastructure) -- rerunning the single failing job (`gh run rerun 34173674038 --job
101898749424`) restored green with no other change. Worth checking whether #1249's fix was
supposed to eliminate this exact signature and, if so, why it still fired.
